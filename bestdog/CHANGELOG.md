## [5.2.5] - 2026-05-18
### Added
- **Feature (Интерактивная детальная анкета Создателя)**: 
  - **Кликабельность карточек альбома**: Теперь при клике на любую открытую карточку в «Альбоме Создателя» открывается потрясающая стеклянная модальная карточка детального анализа настроения персонажа (`#creator-detail-modal`).
  - **Анализ Ауры и Энергии**: Каждая фотография имеет индивидуальный стабильный хэш-код, на основе которого рассчитывается её уникальная Аура (например, *«Космический ультрафиолет»*, *«Грозовой электрик»*), заряд энергии в % и текущий жизненный статус (*«Заряжен на кодинг (200 FPS)»*, *«На чилле»*, *«Сочиняет хит»*).
  - **Ежедневные собачьи предсказания**: В анкету добавлено забавное пророчество от лица создателей, мотивирующее на дальнейшие сканирования.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.2.5` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.2.4] - 2026-05-18
### Removed
- **Feature (Очистка тестового режима)**: Полностью удалена розовая кнопка «ТЕСТ РЕЖИМА» (`#test-creator-theme-toggle`) в левом верхнем углу [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).
- **Refactoring (Удаление мертвого кода)**: Из [script.js](file:///c:/Users/svato/ProgectX/bestdog/script.js) вырезан неиспользуемый обработчик клика для этой кнопки, обеспечивая идеальную чистоту кодовой базы.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.2.4` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.2.3] - 2026-05-18
### Optimized
- **Performance (Устранение лагов при скроллинге карточек коллекции)**:
  - **Аппаратное ускорение контейнера сетки (GPU Grid Compositing)**: Для контейнера сетки `.collection-grid` добавлены свойства `transform: translateZ(0)`, `backface-visibility: hidden` и `will-change: scroll-position`. Это принудительно перенесло весь скроллинг карточек на GPU, разгрузив поток рендеринга процессора.
  - **Аппаратное ускорение отдельных карточек (GPU Card Layers)**: Для каждого `.collection-card` добавлены свойства `transform: translateZ(0)`, `backface-visibility: hidden` и `will-change: transform`, создавая аппаратный кэш для быстрой скроллинг-отрисовки без постоянных вызовов repaint/layout.
  - **Ликвидация фоновых анимаций**: Удалена ресурсоемкая бесконечная анимация пульсации замочков (`pulseLock`) у закрытых карточек. Пульсирование 20+ замочков одновременно нагружало композитор браузера при скроллинге. Заменено на статичный, лаконичный и стильный замок с высокой производительностью.
  - **Ограничение переходов изображений**: Заменен общий `transition: all` на направленный `transition: transform` на изображениях карточек для предупреждения ложных циклов отрисовки.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.2.3` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.2.2] - 2026-05-18
### Added
- **Feature (Mass Signature Overrides #8 to #20)**: Реализована централизованная архитектура подписей Создателя. Внедрена карта `CREATOR_SIGNATURES` для сопоставления первых 20 фотографий Создателя с их именными подписями:
  - 1 — Гений, миллиардер, филантроп
  - 2 — МС СВО
  - 3 — МС ОГУРЕЦ
  - 4 — Банник
  - 5 — Маламчик
  - 6 — 67
  - 7 — Ященко
  - 8 — Гром
  - 9 — Моня
  - 10 — Чарлик
  - 11 — Gazan
  - 12 — Максим Пианист
  - 13 — САЛАМ
  - 14 — Бурмалдатик
  - 15 — Салатик
  - 16 — Хаммам
  - 17 — Майонез
  - 18 — Фли
  - 19 — Черемша
  - 20 — Чуркаслав
- **Refactoring & Optimizations**:
  - Исключен длинный условный код `if-else if` в пользу быстрого сопоставления по URL-ключу за O(1).
  - Написан универсальный цикл авто-санитизатора на старте, очищающий абсолютно все 20 подписей от неверных случайных совпадений у других элементов.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.2.2` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.2.1] - 2026-05-18
### Added
- **Feature (Custom Signature Override #6)**: Закреплена легендарная подпись «67» за шестым Лицом Создателя (`my_faces/face6.png` / Индекс 5).
  - При сканировании шестой фотографии Создателя в ленте через сканер `🤖` теперь гарантированно определяется порода `🐕 67`.
  - В «Альбоме Создателя» для разблокированного шестого слота также отображается эта именная подпись.
  - Добавлена очистка старых случайных совпадений `"67"` у других лиц в авто-санитизатор.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.2.1` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.2.0] - 2026-05-18
### Added
- **Feature (Custom Signature Override #7)**: Закреплена легендарная подпись «Ященко» за седьмым Лицом Создателя (`my_faces/face7.jpg` / Индекс 6).
  - При сканировании седьмой фотографии Создателя в ленте через сканер `🤖` теперь гарантированно определяется порода `🐕 Ященко`.
  - В «Альбоме Создателя» для разблокированного седьмого слота также отображается эта именная подпись.
  - Добавлена очистка старых случайных совпадений `"Ященко"` у других лиц в авто-санитизатор.
### Optimized
- **Performance (Устранение лагов при открытии и прокрутке вверх в Коллекции)**:
  - **Полное удаление полноэкранных blurs на оверлеях**: Убран ресурсоемкий `backdrop-filter: blur(5px)` с главного оверлея модалок (`.modal`), а также `backdrop-filter: blur(6px)` с подложки рейтинга (`.vote-modal-backdrop`). Полноэкранный блюр при прокрутке дочернего грида коллекции заставлял браузер повторно пересчитывать размытие всего экрана на каждый пиксель движения, что вызывало сильнейшие тормоза (особенно при прокрутке вверх).
  - **Премиум-замена**: Блюр заменен на ультра-плавный, глубокий премиальный полупрозрачный фон `rgba(10, 10, 10, 0.88)`, обеспечивающий мгновенный отклик при открытии и стабильные 60 FPS при скроллинге.
  - **Аппаратное ускорение (GPU Acceleration)**: Для контейнера `.modal-content` добавлены CSS-свойства `transform: translateZ(0)` и `backface-visibility: hidden`, что принудительно перенесло рендеринг модального окна на видеокарту. Это полностью устранило jank-эффекты и зависания при прокрутке.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.2.0` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.1.9] - 2026-05-18
### Added
- **Feature (Custom Signature Override #5)**: Закреплена подпись «Маламчик» за пятым Лицом Создателя (`my_faces/face5.jpg`).
  - При сканировании пятой фотографии Создателя в ленте через сканер `🤖` теперь гарантированно определяется порода `🐕 Маламчик`.
  - В «Альбоме Создателя» для разблокированного пятого слота также отображается эта фирменная подпись.
  - Добавлена очистка старых случайных совпадений `"Маламчик"` у других лиц в авто-санитизатор.
### Optimized
- **Performance (Устранение лагов в Альбоме коллекции)**:
  - Полностью удален тяжелый фильтр размытия `blur(3px)` на заблокированных карточках в сетке альбома (`.collection-card.locked .collection-card-img`). Размытие во время прокрутки в переполненном гриде создавало огромную нагрузку на графический процессор. Вместо этого задействован стильный, глубоко затемненный силуэт `brightness(0.12) grayscale(1)`, работающий с частотой 60 FPS.
  - Оптимизированы CSS-переходы для карточек альбома: вместо тяжелого `transition: all 0.3s` прописаны только точечные свойства (`transform`, `border-color`, `box-shadow`), исключая лишние вычисления браузера.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.1.9` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.1.8] - 2026-05-18
### Added
- **Feature (Custom Signature Override #4 - Cache Bump)**: Ресурсный кэш обновлен до `v5.1.8` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.1.7] - 2026-05-18
### Optimized
- **Performance (Eliminated ALL Scroll Lag - Butter Smooth 60 FPS)**:
  - **Вершитель без лагов**: Полностью убран тяжелый `backdrop-filter: blur(5px) !important` со всех карточек собак в режиме Вершителя (`body.vershitel-theme .dog-card`). Карточки теперь рендерятся и скроллятся моментально, сохраняя при этом неоновую фиолетовую рамку и темную тему.
  - **Оптимизация боковых виджетов**: Убраны ресурсоемкие свойства `backdrop-filter` с фиксированных боковых кнопок-виджетов (`.side-widget-compact` и `.king-side-card-compact`), мимо которых пролетают картинки при прокрутке. Они переведены на легкие высокопроизводительные темные полупрозрачные заливки (`rgba(22, 22, 22, 0.88)`).
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.1.7` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.1.6] - 2026-05-18
### Added
- **Feature (Custom Signature Override #4)**: Закреплена легендарная подпись «Банник» за четвертым Лицом Создателя (`my_faces/face4.jpg`).
  - При сканировании четвертой фотографии Создателя в ленте через сканер `🤖` теперь гарантированно определяется порода `🐕 Банник` (вместо случайной выборки).
  - В «Альбоме Создателя» для разблокированного четвертого слота также принудительно отображается эта фирменная подпись.
  - Добавлена очистка старых случайных совпадений `"Банник"` у других лиц в авто-санитизатор.
  - Массив `"банник"` окончательно исключен из общего списка случайных имен Создателя (`CREATOR_BREEDS`).
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.1.6` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.1.5] - 2026-05-18
### Added
- **Feature (Custom Signature Override #3)**: Закреплена легендарная подпись «МС СВО» за вторым Лицом Создателя (`my_faces/face2.jpg`).
  - При сканировании второй фотографии Создателя в ленте через сканер `🤖` теперь гарантированно определяется порода `🐕 МС СВО` (вместо случайной выборки).
  - В «Альбоме Создателя» для разблокированного второго слота также принудительно отображается эта фирменная подпись.
  - Добавлена очистка старых случайных совпадений `"МС СВО"` у других лиц в авто-санитизатор.
  - Массив `"МС СВО"` окончательно исключен из общего списка случайных имен Создателя (`CREATOR_BREEDS`).

### Optimized
- **Performance (Gallery Rendering & Scroll Speed Overhaul)**: Полностью ликвидированы лаги при прокрутке и наведении в главной галерее собак!
  - **Удаление тяжелых размытий**: Сняты ресурсоемкие свойства `backdrop-filter: blur(...)` с часто повторяющихся элементов на карточках собак (боковые кнопки управления `❤️`, `⭐`, `🤖` и золотые плашки `«Новая ✨»`). Вместо этого применены высокопроизводительные темные полупрозрачные заливки с повышенной контрастностью (`rgba(22, 22, 22, 0.88)`). Браузер больше не тратит ресурсы процессора/видеокарты на просчет десятков масок размытия при каждом движении колесика мыши.
  - **Ограничение VanillaTilt**: Анимация трехмерного наклона VanillaTilt теперь инициализируется **только на десктопных устройствах** (ширина экрана > 768px). На мобильных телефонах она отключена, что полностью убрало лаги от прослушивания акселерометра и перерисовок на мобильных браузерах.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.1.5` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.1.4] - 2026-05-18
### Fixed
- **Gameplay (Signature Pools & Savegame Sanitizer)**: Полностью исключен конфликт случайного присвоения закрепленных подписей другим лицам Создателя.
  - **Очистка пулов**: Исключены значения `"гений миллиардер филантроп"` и `"МС ОГУРЕЦ"` из общего массива случайных пород Создателя (`CREATOR_BREEDS`). Теперь они принципиально не могут выпасть другим лицам случайно.
  - **Автоматическая санитизация при старте**: При загрузке страницы скрипт автоматически сканирует сохраненный прогресс игрока в `localStorage`. Если какие-то другие лица (например, Лицо Создателя #16 в вашем сейве) в прошлых сессиях случайно получили подписи «МС ОГУРЕЦ» или «Гений, миллиардер, филантроп» из-за случайного ролла, их порода мягко перераспределяется на корректную случайную породу (например, «Банник», «Лысина» или «МС СВО»). 
  - Это делает закрепленные подписи **абсолютно уникальными и эксклюзивными** только для соответствующих слотов (#1 и #3)!
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.1.4` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.1.3] - 2026-05-18
### Added
- **Feature (Custom Signature Override #2)**: Закреплена подпись «МС ОГУРЕЦ» за третьим Лицом Создателя (`my_faces/face3.jpg`).
  - При сканировании третьей фотографии Создателя в ленте через сканер `🤖` теперь гарантированно определяется порода `🐕 МС ОГУРЕЦ` (вместо случайной выборки).
  - В «Альбоме Создателя» для разблокированного третьего слота также принудительно отображается эта фирменная подпись.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.1.3` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.1.2] - 2026-05-18
### Added
- **Feature (Custom Signature Easter Egg)**: Закреплена легендарная подпись «Гений, миллиардер, филантроп» за первым Лицом Создателя (`my_faces/face1.png`).
  - При сканировании первой фотографии Создателя в ленте через сканер `🤖` теперь гарантированно определяется порода `🐕 Гений, миллиардер, филантроп` (вместо случайной выборки).
  - В «Альбоме Создателя» для разблокированного первого слота также принудительно отображается эта фирменная подпись.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.1.2` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.1.1] - 2026-05-18
### Fixed
- **UI/UX (New Badge Hiding Logic)**: Исправлена логика отображения плашки «Новая ✨» на карточках собак.
  - **Персистентность найденных**: Внедрен массив `userProgress.scanned` для сохранения URL-адресов отсканированных пользователем собак в `localStorage`.
  - **Динамическое скрытие**: Теперь при успешном сканировании породы через сканер `🤖` плашка «Новая ✨» на карточке плавно исчезает в реальном времени (добавляется класс `.hidden`).
  - **Проверка при загрузке**: При генерации новых карточек собак (`createDogCard`) код проверяет, была ли данная собака уже найдена/отсканирована ранее (включая проверку коллекции лиц Создателя). Если да, плашка «Новая ✨» изначально скрывается, наглядно отражая прогресс игрока.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.1.1` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.1.0] - 2026-05-18
### Added
- **Feature (Creator Face Collection System)**: Внедрение полноценной игровой коллекции лиц Создателя («Альбом Создателя»).
  - **Полноэкранный триумфальный оверлей**: При первом сканировании любого из 27 лиц Создателя в ленте собак всплывает невероятно красивый триумфальный полноэкранный оверлей с фейерверком из конфетти, анимацией разблокировки персонажа и возможностью моментально перейти в альбом.
  - **«Альбом Создателя»**: Добавлено роскошное модальное окно с неоновым прогресс-баром и адаптивной сеткой из 27 карт. Закрытые карточки отображаются как загадочные размытые силуэты с иконками замков (`🔒`) и подписью `Лицо Создателя #X`, а открытые — во всей красе с неоновым свечением, сканированной породой и точной датой поимки.
  - **Виджеты навигации**: Кнопка вызова коллекции добавлена на левую панель меню (иконка 📸) на десктопе, а также в мобильную панель управления.
  - **Достижение «Король Создателей»**: Новая глобальная ачивка `full_collection_achievement`, требующая собрать все 27 лиц создателя в свой альбом.
### Fixed
- **Gameplay (Creator Photos Spawn Rate Deadlock Fix)**: Исправлена логическая тупиковая ситуация (deadlock) с шансом появления фотографий.
  - Ранее лица Создателя могли спавниться только при активном Режиме Вершителя, но открыть сам Режим можно было только найдя и открыв хотя бы одну фотографию Создателя в ленте (замкнутый круг при очистке `localStorage`).
  - Настроено согласно предпочтениям: вероятность спавна теперь составляет **12%** в любом базовом режиме (позволяет быстро находить и открывать новые карточки) и повышается до **20%** при включённом Режиме Вершителя.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.1.0` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html) и скриптах.

## [5.0.52] - 2026-05-18
### Fixed
- **UI/UX (Cursor Toggle Menu Visibility Fix)**: Исправлена ошибка, из-за которой меню переключения кастомного курсора Создателя не появлялось после его покупки в магазине.
  - **Исправление логики видимости**: В [script.js](file:///c:/Users/svato/ProgectX/bestdog/script.js) в функции `window.checkUnlockedMemes` добавлено условие, проверяющее покупку курсора `userProgress.purchased?.includes('paw_cursor')`. Теперь контейнер эффектов и тем `.meme-hub-container` (кнопка ✨) корректно отображается при покупке курсора, даже если у пользователя ещё не разблокированы другие секретные достижения (Огурец Рик, кроссовки Нью Бэланс и т.д.).
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.52` в [index.html](file:///c:/Users/svato/ProgectX/bestdog/index.html).

## [5.0.51] - 2026-05-17
### Fixed
- **UI/UX (Trophy Button Toggle & Leaderboard Modal State Polish)**: Исправлена UX-недоработка в мини-игре — кнопка трофея (кубка) теперь работает как полноценный переключатель (Toggle).
  - **Двусторонний переключатель (Toggle)**: В [script.js](file:///c:/Users/svato/bestdog/script.js) функция `window.showLeaderboard` переписана так, чтобы при повторном клике на иконку кубка `#open-leaderboard-btn` панель «Топ Кинологов» `#game-leaderboard-view` корректно скрывалась, а не перезагружалась.
  - **Централизованный хелпер закрытия**: Внедрен метод `window.hideLeaderboard`, который добавляет класс `hidden` к панели топа и гарантированно возвращает видимость крестику закрытия основной игры (`#game-modal .close-modal`), удаляя у него класс `element-hidden`.
  - **Интеграция с закрытием игры**: Обновлен обработчик `window.closeGameModalDirectly` — теперь при закрытии основного окна игры панель лидеров также принудительно скрывается через `window.hideLeaderboard()`, предотвращая сохранение открытых панелей при следующем запуске игры.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.51` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.50] - 2026-05-17
### Added
- **Feature (Double Balance Wallet in Modal)**: Внедрение функционала двойного баланса в модальном окне «Твой баланс».
  - **JS/HTML Интеграция**: В [index.html](file:///c:/Users/svato/bestdog/index.html) устаревшая разметка огромного одиночного числа заменена на современный гибкий контейнер `.wallet-container`, содержащий две карточки `.wallet-item` с уникальными ID: `#modal-bones-val` (для обычных косточек 🦴) и `#modal-superlikes-val` (для премиальных Супер-лайков 💖).
  - **Динамическая отрисовка**: В [script.js](file:///c:/Users/svato/bestdog/script.js) при открытии модального окна баланса значение Супер-лайков динамически подтягивается из прогресса пользователя `userProgress.superLikes || 0` и подставляется в разметку наряду с основным балансом костей.
  - **Современная CSS-Стилизация**: В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлены классы `.wallet-container` (Flexbox с распределением по ширине), `.wallet-item` (карточки с полупрозрачным фоном, отступами и скруглением), `.wallet-value` и `.wallet-icon`.
  - **CSS-Защита Режима Вершителя**: В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлены жесткие переопределения для `body.vershitel-theme .wallet-item` (темно-фиолетовый фон, неоновая рамка и свечение) и `body.vershitel-theme .wallet-value` (белый цвет текста и розовая неоновая тень), что предотвращает появление черных или нечитаемых элементов.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.50` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.49] - 2026-05-17
### Fixed
- **UI/UX (Header Control Fixed Position Hotfix)**: Полное и окончательное исправление бага "уползания" панели управления (баланса и переключения темы) вверх при прокрутке в Режиме Вершителя.
  - **Диагностика и устранение корневой причины**: Обнаружено, что свойство `backdrop-filter: blur(5px) !important`, заданное для родительского элемента `header` в Режиме Вершителя, создавало новый контекст форматирования (containing block). Это превращало `position: fixed` у дочерних элементов в относительное позиционирование, заставляя их прокручиваться.
  - **Реструктуризация HTML**: В [index.html](file:///c:/Users/svato/bestdog/index.html) виджет баланса `#global-bones` вынесен из контейнера `<header>` на уровень корня `<body>` (вслед за кнопкой смены темы `#theme-toggle`), что исключило любое влияние родительских CSS-фильтров.
  - **Принудительные CSS-правила**: В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлены абсолютные, ультимативные правила фиксации `.theme-toggle`, `.bones-wallet` и `#global-bones` в Режиме Вершителя с наивысшим приоритетом (`position: fixed !important`, `z-index: 9999 !important`), жестко закрепив их в правом верхнем углу (на стандартных координатах `top: 20px`, `right: 20px` / `right: 80px`).
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.49` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.47] - 2026-05-17
### Fixed
- **UI/UX (Gallery Flex Wrap Centering Overhaul)**: Устранены некрасивые визуальные «дыры» и пустые места в последнем ряду галереи собак.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) жесткая сетка Grid заменена на гибкий Flexbox с переносом строк (`display: flex !important`, `flex-wrap: wrap !important`) и центрированием элементов последнего неполного ряда (`justify-content: center !important`).
  - Установлены жесткие ограничения размеров карточек (`flex: 0 1 260px !important`, `max-width: 260px !important`), предотвращающие раздувание Flexbox-элементов на широких экранах.
- **Gameplay (Broken Image Card Auto-Hiding)**: Исключено появление пустых невидимых блоков в центрированном Flexbox-контейнере.
  - В [script.js](file:///c:/Users/svato/bestdog/script.js) в функции `createDogCard` логика `onerror` переписана так, чтобы при полной невозможности загрузить изображение (даже после применения fallback-адреса) весь элемент `.dog-card` получал стиль `display: none !important`.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.47` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.46] - 2026-05-17
### Fixed
- **UI/UX (Class Misplacement & Balance Border Hotfix)**: Полное и окончательное исправление визуальных багов с отображением цифр баланса.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлена безусловная CSS-броня на дочерние элементы виджетов баланса (`span`, `div` внутри `[id*="balance"]` и `.balance-btn`), которая принудительно обнуляет рамки (`border: none !important`), фон (`background: transparent !important`) и тени (`box-shadow: none !important`).
  - В [script.js](file:///c:/Users/svato/bestdog/script.js) исправлено распределение классов в функции `updateUI`: контейнер `#global-bones` получает `.force-dark-bg`, а текстовый span `#bones-balance` получает `.force-white-text` с удалением `.force-dark-bg`, что исключает появление серых/фиолетовых квадратов вокруг цифр.
- **UI/UX (Shop Sidebar Button Armor)**: Устранено периодическое окрашивание кнопки Магазина (`#shop-btn`) в белый цвет при обновлении интерфейса.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлена вечная CSS-броня для `#shop-btn`, `.sidebar-shop-btn` и иконок магазина в Режиме Вершителя, фиксирующая темный неоновый фон и свечение.
  - В [script.js](file:///c:/Users/svato/bestdog/script.js) в функции `fixVisualBugsDynamically` кнопка магазина принудительно наделяется классом `.force-dark-bg` с полной очисткой inline-стилей, которые могли красить её в белый цвет.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.46` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.45] - 2026-05-17
### Fixed
- **UI/UX (Rating Modal Outline & Border-Radius Fix)**: Исправлена проблема квадратных обводок и острых углов у кнопок, вкладок и выпадающих списков (`select`) в окне рейтингов в Режиме Вершителя.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлены жесткие оверрайды `outline: none !important` и `border-radius: 8px !important` для всех элементов управления внутри `.rating-modal-inner` и `.rating-filters`. Вкладкам-переключателям `.tab-button` задано идеальное круглое скругление `border-radius: 20px !important`.
- **Gameplay (Ascending Sorting & Empty Placeholders)**: Исправлен критический баг исчезновения собак при выборе сортировки «Сначала худшие» (Ascending).
  - В [script.js](file:///c:/Users/svato/bestdog/script.js) алгоритм сортировки полностью переведен на клиентскую сторону. Все документы загружаются и сортируются в JS с обязательным приведением к числам `Number(a.score) - Number(b.score)` и `Number(a.rating) - Number(b.rating)`. Это полностью исключает обнуление массива при лимитировании запроса на стороне Firestore.
  - Пустая заглушка в окне рейтингов стала умной и динамической: при фильтре по общему баллу выводится текст «Пока нет оценок», а при фильтре по лайкам — «Пока никто не набрал лайков».
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.45` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.44] - 2026-05-17
### Fixed
- **Режим Вершителя (Ультимативные классы)**: Полное и бескомпромиссное устранение визуальных багов с белым и черным нечитаемым текстом при активном Режиме Вершителя.
  - **Добавление абсолютных правил в CSS (ШАГ 1)**: В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлены два абсолютных класса: `.force-dark-bg` (для глубокого фиолетового фона, неоновой рамки и свечения) и `.force-white-text` (для белого текста с ярким неоновым свечением).
  - **Жесткое внедрение классов через JS (ШАГ 2)**: В [script.js](file:///c:/Users/svato/bestdog/script.js) эти классы принудительно добавлены на три ключевых элемента:
    1. Кнопка баланса в шапке (`global-bones`) в функции `updateUI` получает класс `.force-dark-bg`.
    2. Большая цифра баланса в окне «Твой баланс» (`modal-bones-val`) при открытии получает класс `.force-white-text`.
    3. Текст множителя и подсказок в игре («Следующий ответ...» / `.game-reward-banner`, `current-reward-val` и `multiplier-badge`) при старте и обновлении раундов получают класс `.force-white-text`.
- **UI/UX (Grid Gap & Button Placement Bug)**: Полное и окончательное исправление пустот в галерее карточек собак и позиционирования кнопки «Смотреть ещё собак».
  - **Разделение в HTML**: В [index.html](file:///c:/Users/svato/bestdog/index.html) контейнер `.dogs-grid-container` очищен от посторонней верстки: кнопка вынесена строго наружу в независимый блок `.load-more-wrapper`.
  - **Бетонный CSS Grid**: В [style.css](file:///c:/Users/svato/bestdog/style.css) полностью удален Flexbox у контейнера собак и задан жесткий, адаптивный CSS Grid (`display: grid !important`, `grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)) !important`, `gap: 20px !important`). Карточки теперь выстраиваются в безупречные ровные ряды. Блок кнопки `.load-more-wrapper` отцентрирован строго под сеткой с помощью `display: flex !important` и `justify-content: center !important`.
  - **Защита от призраков**: В [script.js](file:///c:/Users/svato/bestdog/script.js) добавлено строгое условие-предохранитель в цикле `loadDogsBatch`. Теперь в сетку гарантированно не могут добавиться пустые, скрытые или невалидные карточки при сбоях сети или API.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.44` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.43] - 2026-05-17
### Fixed
- **UI/UX (Bones Wallet & Game Stats UI Polish)**: Финальная полировка интерфейса в Режиме Вершителя.
  - **Затемнение баланса косточек (ШАГ 1)**: В [style.css](file:///c:/Users/svato/bestdog/style.css) точечно зафиксирован глубокий темно-фиолетовый неоновый стеклянный фон `rgba(20, 5, 35, 0.95)` для капсулы баланса косточек в шапке `.bones-wallet`. Применен ярко-розовый/фиолетовый неоновый цвет `#e066ff !important` для цифр баланса (`.bones-wallet *`), предотвращая их сброс в серый или зеленый цвет.
  - **Устранение лишних обводок в игре (ШАГ 2)**: Отменены нежелательные рамки, контуры, тени и фоны для элементов статистики в окне игры «Угадай породу» (`.game-modal .game-stats-score`, `.stats-header`, `.score-container`, `.streak-container`, `.game-stats-bar`, `.game-stat`, `#game-score`, `#game-streak`), которые ошибочно наследовались от глобального неонового оверрайда.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.43` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.42] - 2026-05-17
### Fixed
- **UI/UX (Game UX Cleanup & Contrast Fix)**: Полное косметическое исправление и реставрация панели игровых множителей и бонусов скорости в игре «Угадай породу».
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлено глобальное правило, возвращающее идеальную контрастность тексту множителей (`.multiplication-bonuses`, `.multiplication-factor`, `.bonus-status-text`, `.speed-bonus-info` и дочерним спанам): текст теперь горит ярко-белым цветом `#ffffff !important` с легким неоновым свечением `text-shadow`.
  - Устранена проблема неаккуратной блочной структуры: контейнеры множителей и бонусов скорости (`.multiplier-container`, `.multiplier-row`, `.bonuses-grid`) перестроены во флекс-ряд с аккуратным распределением, легкой неоновой подложкой `rgba(30, 20, 50, 0.5)` и тонкой фиолетовой рамкой.
  - Каждая отдельная ячейка множителя (`.multiplier-cell`, `.bonus-box`) лишена грубых рамок и сведена к аккуратному минималистичному виду.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.42` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.41] - 2026-05-17
### Fixed
- **UI/UX (Strict Class Overrides for Sensitive Dark Blocks)**: Намертво зафиксирован темный неоновый стиль для двух важнейших элементов интерфейса, которые сбрасывались в белый цвет.
  - На основе глубокого анализа HTML-разметки и JS-генераторов определены точные классы контейнеров: капсула баланса косточек в шапке (`.bones-wallet`) и нижние панели с информацией о породе и лайках под фотографиями в модальных окнах (`.king-info` в окне Короля недели, `.hall-info` в карточках номинантов Зала Славы и `.rating-info` в строках рейтингов).
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлен жесткий оверрайд, гарантирующий темно-фиолетовый фон `rgba(20, 5, 35, 0.95)`, фиолетовую границу и неоновое свечение для этих элементов в Режиме Вершителя.
  - Прописано универсальное правило осветления дочерних элементов (`.bones-wallet *`, `.king-info *`, `.hall-info *`, `.rating-info *`), которое полностью предотвращает появление нечитаемого черного текста внутри этих блоков.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.41` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.40] - 2026-05-17
### Fixed
- **UI/UX (Text Contrast Bug in Hall of Fame & Breed Battle)**: Исправлен критический баг нечитаемости текста в Зале Славы и Битве пород при активном Режиме Вершителя.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлено масштабное правило, перекрашивающее текстовые элементы карточек номинантов и таблиц битвы пород (`.hall-card`, `.hall-rank`, `.hall-breed`, `.hall-likes`, `.breed-rank-item`, `.breed-rank-num`, `.breed-rank-name`, `.breed-rank-likes`, `.breed-rank-info`, `.breed-rank-info div`) в ярко-белый цвет `#ffffff !important` с добавлением мягкого текстового свечения `text-shadow: 0 0 4px rgba(255, 255, 255, 0.2) !important` для идеальной читаемости на темном неоновом фоне.
  - Обеспечено осветление заголовков и служебных иконок внутри модального окна Зала славы (`#hall-modal .modal-title`, `#hall-modal .modal-icon`, `.hall-content .title`, `.hall-content .subtitle`).
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.40` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.39] - 2026-05-17
### Fixed
- **UI/UX (Final Wrappers & Containers Neon Theme Cleanup)**: Полная и окончательная зачистка оставшихся белых контейнеров-оберток на сайте.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлено точечное правило для всех элементов-подложек и оберток: нижних частей карточек собак (`.dog-info`, `.dog-card-bottom`, `.dog-card-footer`, `.dog-details-content`), внешнего контейнера косточек в шапке (`.bones-wallet`, `.balance-container`, `.header-bones-wrapper`), подложки фильтров рейтингов (`.filter-bar`, `.filters-container`, `.sort-controls-wrapper`, `.rating-filters`), а также плашек породы на карточках (`.new-badge`, `.breed-info-panel`).
  - Все эти элементы в Режиме Вершителя получили темно-фиолетовый неоновый полупрозрачный фон `rgba(20, 5, 35, 0.95)`, неоновую рамку, фиолетовое свечение `box-shadow` и скругленные углы `border-radius: 8px !important`.
  - Текст породы и надписи на этих темных подложках (`.dog-info h3`, `.dog-name`, `.breed-title`, `.new-badge`, `.breed-info-panel`) жестко перекрашены в чистый белый цвет `#ffffff !important` для великолепной читаемости.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.39` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.38] - 2026-05-17
### Fixed
- **UI/UX (Final Dark Containers & Menus Super-Override)**: Полностью искоренены белые "радиационные" зоны внутри выпадающих меню, карточек, рейтинговых таблиц и модальных окон в Режиме Вершителя.
  - В самый конец [style.css](file:///c:/Users/svato/bestdog/style.css) добавлено глобальное супер-правило оверрайда, охватывающее все внутренние контентные элементы: выпадающие селекты поиска/фильтрации (`select`, `.filter-select`, `.rating-dropdown`), элементы рейтинговых списков (`.rating-item`, `.rank-row`), элементы лидербордов (`.leader-item`), карточки достижений (`.ach-card`), карточки товаров в магазине (`.shop-item`), общие счетчики (`.bone-counter`, `.bones-display`), а также внутренние блоки описаний (`.modal-body-content`, `.dog-details-info`, `.rating-modal-inner` и дочерние элементы `[class*="rating-window"]`).
  - Все эти контейнеры в Режиме Вершителя получили глубокий темный неоновый фон `rgba(20, 5, 35, 0.9)`, фиолетовые надписи `#e066ff`, неоновую фиолетовую окантовку и легкое неоновое свечение.
  - Намертво зафиксирован темный фон для выпадающих опций внутри списков (`select option`) с глубоким цветом `#140028`, гарантируя отличную читаемость на мобильных и десктопах.
  - Текстовое наполнение внутри белых в прошлом областей (`.rating-item span`, `.rank-row td`, `.dog-card-title`, `.leader-item span`, ачивки и тексты магазина) жестко переключено на чистый светлый цвет `#ffffff !important`.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.38` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.37] - 2026-05-17
### Added
- **UI/UX (Verdict Rating Live Average Score)**: Добавлена новая интерактивная фича — динамический средний балл («Итог») в окне оценки собаки.
  - В [index.html](file:///c:/Users/svato/bestdog/index.html) под фотографией собаки в панели `.vote-left` добавлена разметка `.total-rating-badge.score-badge` со счетчиком `#live-average-score` для вывода динамического балла.
  - В [script.js](file:///c:/Users/svato/bestdog/script.js) написана глобальная функция `window.calculateAverageScore()`, которая мгновенно суммирует значения 5 ползунков критериев оценки, делит сумму на 5 и обновляет элемент с округлением до одного знака после запятой (`.toFixed(1)`).
  - В [script.js](file:///c:/Users/svato/bestdog/script.js) на каждый ползунок критерия при рендеринге повешен мгновенный вызов `window.calculateAverageScore()` на событие `oninput`, а также обеспечен автоматический расчет дефолтного значения (`5.0`) при первом открытии окна оценки.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) плашка сверстана с красивыми скруглениями, мягкой тенью и бирюзово-зеленым неоновым шрифтом для вывода балла. В Режиме Вершителя она автоматически получает стильный фиолетовый оверрайд, неоновую рамку и фиолетовое свечение благодаря наследованию класса `.score-badge`.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.37` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.36] - 2026-05-17
### Fixed
- **UI/UX (Global Neon Carpet Bombing in Creator Mode)**: Реализован тотальный, бескомпромиссный неоновый оверрайд для Режима Вершителя.
  - В самый конец [style.css](file:///c:/Users/svato/bestdog/style.css) добавлено масштабное CSS-правило, которое на корню пресекает сброс фона любых интерактивных элементов в светлые тона.
  - Оверрайд применен ко всем кнопкам (`button`, `.btn`, `[role="button"]`), вкладкам (`.tab-btn`, `.tab-button`, `.tab-item`), счетчику косточек в шапке (`.balance-container`, `.bone-counter`), плашкам счетов (`.score-badge`), вариантам ответов в мини-игре (`.game-option-btn`), а также панелям игровой статистики (`.game-stats-bar`, `.game-stat`).
  - Все эти элементы получили великолепное оформление: глубокий темный полупрозрачный фон `rgba(15, 5, 30, 0.85)`, яркий неоновый фиолетовый текст/иконки `#e066ff`, неоновую фиолетовую рамку и фиолетовое свечение `box-shadow` с эффектом размытия заднего плана.
  - Написаны универсальные, сглаженные эффекты наведения (`:hover`) с переходом в плотный неоновый фиолетовый цвет с увеличенным свечением.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.36` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.35] - 2026-05-17
### Fixed
- **UI/UX (Emergency Rollback & Game Modal Fix)**: Исправлена критическая регрессия с автоматическим открытием игры и блокировкой сайта на старте.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) исправлено правило позиционирования игрового оверлея `#game-modal.modal` на `#game-modal.modal.active` с возвратом дефолтного значения `display: none` для неактивного состояния. Это полностью устранило баг "вечно открытой" игры.
  - В [script.js](file:///c:/Users/svato/bestdog/script.js) переменная `isDark` внутри `fixVisualBugsDynamically` расширена проверкой `isVershitel`, что предотвращает перекрашивание других кнопок (включая игровые) в белый цвет при активном Режиме Вершителя.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) изолированы правила оверрайдов для кнопок Вершителя (теперь строго по конкретным ID: `#theme-toggle`, `#theme-btn`, `#shop-btn`, `#side-shop-btn`, `#mob-shop-btn`), исключая побочное влияние на элементы игры.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) восстановлен оригинальный темный неоновый стиль для плашки очков в мини-игре (`#game-modal .game-stats-bar`, `#game-modal .game-stat`).
- **UI/UX (Invulnerable Close Button)**: Крестик закрытия игры (`#close-game`) сделан абсолютно отказоустойчивым.
  - В [index.html](file:///c:/Users/svato/bestdog/index.html) крестику добавлен уникальный ID `id="close-game"` и прямой, независимый от внешних файлов, inline-обработчик клика в качестве резервного фаллбэка.
  - В [script.js](file:///c:/Users/svato/bestdog/script.js) создана и экспортирована чистая функция `window.closeGameModalDirectly()`, которая мгновенно деактивирует модальное окно и полностью разблокирует скролл страницы.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.35` в [index.html](file:///c:/Users/svato/bestdog/index.html) для мгновенного сброса браузерного кэша.

## [5.0.34] - 2026-05-17
### Fixed
- **UI/UX (Creator Theme Controls Fix)**: Устранена проблема сброса стилей кнопок магазина и переключателя темы в Режиме Вершителя.
  - В [script.js](file:///c:/Users/svato/bestdog/script.js) функция `fixVisualBugsDynamically` обновлена: теперь она полностью пропускает инлайновые стилизации переключателя темы (`#theme-toggle`) и кнопок магазина (`#side-shop-btn`, `#mob-shop-btn`), когда активен `vershitel-theme`, а также очищает старые инлайновые переопределения для обеспечения чистого рендеринга через CSS.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлены жесткие ультимативные правила оверрайда стилей кнопок в Режиме Вершителя с красивыми неоново-фиолетовыми цветами (`#e066ff`), полупрозрачным фиолетовым фоном, неоновой обводкой, эффектами свечения (`box-shadow`) и размытия `backdrop-filter: blur(4px)`.
  - Добавлены соответствующие плавные переходы при наведении на эти кнопки.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.34` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.33] - 2026-05-17
### Fixed
- **UI/UX (True Full-Fit Modal Fix)**: Переработана архитектура позиционирования игрового окна для идеального центрирования на любых дисплеях.
  - У модального окна игры `.game-modal` удалены фиксированные свойства `top` и `transform`, вызывавшие смещения за границы экрана.
  - Центрирование теперь осуществляется через вертикальный Flexbox на родительском темном оверлее (`#game-modal` / `.game-modal-backdrop`), что гарантирует идеальное центрирование без необходимости скролла.
  - Максимальная высота содержимого модального окна `.modal-content` увеличена до `95vh` с принудительной блокировкой `overflow: hidden`, а фиксированная высота картинки собаки уменьшена до идеальных `180px` (с `min-height: 180px` и `object-fit: contain`).
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.33` в [index.html](file:///c:/Users/svato/bestdog/index.html) для мгновенного обновления у всех пользователей.

## [5.0.32] - 2026-05-17
### Fixed
- **UI/UX (Error Placeholder Overlap Bug Fix)**: Полностью решена проблема наложения плашки «Новая» на текст ошибки при сбое загрузки картинок (когда сайт открывается без VPN).
  - В [script.js](file:///c:/Users/svato/bestdog/script.js) доработан обработчик `onerror`: теперь при двойном сбое (основной URL и Unsplash-зеркало) картинка скрывается, а на ее месте динамически создается чистый контейнер `.error-placeholder.network-error-text` с текстом ошибки.
  - В [style.css](file:///c:/Users/svato/bestdog/style.css) прописаны правила позиционирования: контейнер ошибки позиционируется абсолютно, растягивается на 100% карточки с красивым темным фоном `#1a1a24`, флекс-центрируется по осям и имеет отступ сверху `padding-top: 55px !important`, что полностью исключает перекрытие текста badge-компонентом «Новая».
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.32` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.31] - 2026-05-17
### Fixed
- **UI/UX (Fixed Frame & Anti-Duplicate Fix)**: Полный отказ от генерации дублирующей кнопки `#btn-game-next` в [script.js](file:///c:/Users/svato/bestdog/script.js). Теперь при завершении раунда по таймеру или ответу правильно и гарантированно отображается только одна штатная статическая кнопка продолжения `#next-round-btn`, без дублирования элементов.
- **UI/UX (Rigid Image Frame)**: Внедрена строго фиксированная «коробка» для картинок собак в игровом окне (`height: 200px !important`, `max-height: 200px !important`, `object-fit: contain !important`, сплошной темный фон `#1a1a24 !important` для заполнения пустот). Это предотвращает деформацию и расползание интерфейса из-за горизонтальных или вертикальных картинок собак.
- **UI/UX (Locked Modal Height)**: Высота игрового окна жестко зажата до `max-height: 85vh !important` с абсолютным запретом прокрутки (`overflow: hidden !important`), благодаря чему весь компактный интерфейс идеально влезает на экраны любых размеров.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.31` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.30] - 2026-05-17
### Fixed
- **UI/UX (Vertical Compact UI Polish)**: Масштабное сжатие игрового окна для предотвращения вертикального переполнения и исключения необходимости скролла. Добавлены строгие компактные стили в [style.css](file:///c:/Users/svato/bestdog/style.css):
  - Высота картинки собаки `#game-modal .target-dog` уменьшена с 280px до **220px**.
  - Внешние и внутренние отступы модалки сжаты до `gap: 8px !important` и `padding: 15px !important`.
  - Уменьшена высота плашек множителя (`#game-modal .game-reward-banner`) и результатов (`#game-modal .game-feedback`) с уменьшением отступов до `4px 0 !important` и шрифта до `14px !important`.
  - Сетка кнопок сведена к `gap: 8px !important` и `margin-bottom: 10px !important`, а сами кнопки ответов и кнопка продолжения урезаны по высоте (`min-height: 44px !important`, `padding: 10px !important`, `font-size: 14px !important`).
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.30` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.29] - 2026-05-17
### Fixed
- **UI/UX (Game Modal Overhaul)**: Полный пересмотр механики завершения раундов в мини-игре «Угадай породу». При истечении времени по таймеру или при неверном/верном ответе теперь динамически генерируется и аккуратно встраивается в самый низ игрового окна кнопка «Следующий раунд ➡️». Это предотвращает зависание игрока в модальном окне и делает игровой процесс непрерывным.
- **UI/UX (Modal Responsive Layout)**: Косметическое исправление верстки игрового окна в [style.css](file:///c:/Users/svato/bestdog/style.css). Заменены некорректные селекторы на реальные (`#game-modal .modal-content` и `#game-modal .target-dog`). Установлено адаптивное ограничение высоты картинки собаки (`max-height: 280px !important`, `object-fit: contain !important`) и добавлена прокрутка содержимого при переполнении (`max-height: 90vh !important`, `overflow-y: auto !important`). Теперь картинки выглядят премиально, не сжимаются и не выходят за рамки экрана.
- **Cache Bumping**: Версия кэша ресурсов поднята до `5.0.29` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.28] - 2026-05-17
### Fixed
- **Gameplay (Forced Drop Rate Fix)**: Внедрен абсолютно бронебойный дроп лиц Создателя в цикле `loadDogsBatch`. Теперь статус проверяется комплексно через классы на `body` (`vershitel-theme`, `creator-theme`, `creator-mode-active`) и персистентную запись `localStorage.getItem('creator_mode_enabled')`, гарантируя появление лиц Создателя с вероятностью `12.5%` при любом запуске.
- **UI/UX (Next Round Button Fix)**: Исправлена логика показа кнопки «Продолжить» (`next-round-btn`) в игре «Угадай породу». Изменено ошибочное скрытие `.classList.add('hidden')` на правильное отображение `.classList.remove('hidden')` при выборе любого ответа, полностью разблокируя игровой процесс.
- **UI/UX (Modal Image Stretch Fix)**: Разрешен конфликт CSS-специфичности с ID `#game-main-img`. В [style.css](file:///c:/Users/svato/bestdog/style.css) добавлено жесткое принудительное ограничение высоты картинки (`height: 200px !important`, `object-fit: cover !important`) непосредственно по селектору ID, предотвращая растягивание картинок на всю высоту экрана и обеспечивая 100% видимость кнопок на всех устройствах.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.28` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.27] - 2026-05-17
### Fixed
- **UI/UX (Modal Windows Polish)**: Устранен критический баг пустого модального окна и заблокированной игры. Написана и внедрена глобальная JS-функция `window.openUniversalModal(headerText, contentElement, showContinueButton = true)`, которая динамически создает или переиспользует `#universal-modal` и жестко вшивает интерактивную кнопку «Продолжить» (`id="btn-modal-continue"`), если флаг равен `true`.
- **UI/UX (Rating Modal Continue Button)**: В [index.html](file:///c:/Users/svato/bestdog/index.html) внутрь `#rating-modal` статически внедрена кнопка «Продолжить» (`id="btn-modal-continue"`) для гарантированного закрытия и разблокировки игры в любых сценариях.
- **UI/UX (Dog Image Size Fix in Modals)**: Добавлен класс `class="target-dog"` на целевое изображение собаки `#game-main-img` в игре "Угадай породу". В [style.css](file:///c:/Users/svato/bestdog/style.css) прописаны строгие правила `.modal-content img.target-dog` с фиксированной высотой `200px` и `object-fit: cover`, предотвращая расползание картинок на весь экран и обеспечивая 100% видимость кнопок ответов на всех устройствах.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.27` в [index.html](file:///c:/Users/svato/bestdog/index.html) для сброса кэша.

## [5.0.26] - 2026-05-17
### Fixed
- **Gameplay (True Flag Synchronization)**: Исправлена логика определения Режима Вершителя в цикле подгрузки собак. Вместо проверки динамического класса на `body` (который мог не успеть инициализироваться или сбрасывался при обновлении страницы) используется надёжное глобальное состояние `userProgress.creatorModeActive` и значение из `localStorage.getItem('creator_mode_enabled')`.
- **Theme Stabilization**: При разблокировке ачивки `creator_face_achievement`, а также при кликах по кнопкам `creatorThemeBtn` и `testThemeBtn`, переменная `userProgress.creatorModeActive` и локальное хранилище `creator_mode_enabled` синхронизируются на `true` / `false` и сохраняются через `save()`.
- **UI Persistence**: В функцию `updateUI()` внедрено автоматическое восстановление Режима Вершителя (`vershitel-theme`) на `body` при старте приложения, если этот режим был активирован ранее.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.26` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.25] - 2026-05-17
### Fixed
- **UI/UX (Grid Layout Hole Bug)**: Устранен баг пустых мест и дыр в сетке галереи при обрывах сетевых запросов. Галерея полностью переведена на единую монолитную архитектуру CSS-сетки (**Flat Grid Architecture**).
- **Structure**: Главный контейнер переименован с `dog-grid` на `#dogs-container` в [index.html](file:///c:/Users/svato/bestdog/index.html), полностью исключая создание любых промежуточных оберток для батчей.
- **JS & CSS**: В [script.js](file:///c:/Users/svato/bestdog/script.js) новые карточки `.dog-card` добавляются через `grid.appendChild` напрямую в единый глобальный контейнер. В [style.css](file:///c:/Users/svato/bestdog/style.css) для `#dogs-container` заданы жесткие неоновые правила автоматического заполнения колонок `repeat(auto-fill, minmax(280px, 1fr))`, благодаря чему браузер автоматически и бесшовно заполняет пустые ячейки при последующих подгрузках.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.25` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.24] - 2026-05-17
### Fixed
- **Gameplay (Forced Drop Rate Fix)**: Переписана и ужесточена логика замешивания лиц Создателя в общую ленту собак. Для полной надежности и соответствия требованиям пользователя, в `loadDogsBatch` объявлен массив `dogImagesArray` и принудительно синхронизируется флаг `userProgress.creatorModeActive` с текущим состоянием темы на `body`.
- **Diagnostics**: Внедрен детальный `console.log("🎲 [РЕЖИМ ВЕРШИТЕЛЯ]: Выпало лицо Создателя ->", imgSrc)` для наглядного отслеживания выпадающего рандома в консоли разработчика.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.24` в [index.html](file:///c:/Users/svato/bestdog/index.html) для сброса кэша браузера.

## [5.0.23] - 2026-05-17
### Fixed
- **UI/UX (Smooth Scroll Lock)**: Полностью переработана система блокировки скролла при открытых модальных окнах для исключения резких прыжков фона страницы. В [style.css](file:///c:/Users/svato/bestdog/style.css) класс `.no-scroll` сокращен до простого `overflow: hidden !important` (удалены `height`, `width`, `position` и `touch-action`).
- **Optimization**: В [script.js](file:///c:/Users/svato/bestdog/script.js) удалена сложная математика сохранения `window.scrollY` и позиционирования fixed, вызывавшая прыжки. Вместо этого в `pushModalState` вычисляется ширина системного скроллбара (`window.innerWidth - document.documentElement.clientWidth`), которая накладывается на `body.style.paddingRight` при открытии модального окна и сбрасывается при закрытии, предотвращая любое горизонтальное и вертикальное смещение контента.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.23` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.22] - 2026-05-17
### Changed
- **Gameplay (Drop Rate Buff)**: Существенно увеличен шанс появления фотографий Создателя из массива `CREATOR_PHOTOS` при генерации карточек в ленту собак. В Режиме Вершителя вероятность установлена строго на **12.5%** (соотношение 1 к 8) через условие `creatorModeActive && (Math.random() < 0.125)`.
### Removed
- **UI/UX (Zoom Markup Cleanup)**: Полностью удален неиспользуемый устаревший тег `<div id="game-image-zoom-overlay">` из [index.html](file:///c:/Users/svato/bestdog/index.html), который вызывал отображение битой иконки с alt "zoom" в левом нижнем углу экрана.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.22` в [index.html](file:///c:/Users/svato/bestdog/index.html) для принудительного обновления у всех пользователей.

## [5.0.21] - 2026-05-17
### Fixed
- **UI/UX (Context Conflict Fix)**: Исправлен баг ложного считывания породы на дублирующихся изображениях. Функция `window.scanBreed` переведена на использование контекста события клика (`event`): теперь `img.src` и панели результатов определяются строго внутри контейнера нажатой карточки `.dog-card`, полностью исключая конфликты ID и путей.
- **UI/UX (Scroll Jump Prevention)**: Устранен мгновенный прыжок скролла вверх при закрытии модальных окон (включая окно оценки). В функцию открытия `pushModalState` добавлено запоминание точных координат `window.scrollY` перед активацией блокирующего класса `.no-scroll` на `body`. При закрытии окна в `closeAnyModal` и обработчике `popstate` координаты скролла бесшовно восстанавливаются. На кнопках закрытия добавлен `e.preventDefault()`.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.21` в [index.html](file:///c:/Users/svato/bestdog/index.html) для сброса кэша браузера.

## [5.0.20] - 2026-05-17
### Added
- **UI/UX (Custom Cursor Toggle)**: Реализован интерактивный переключатель (тумблер) для включения/выключения кастомного курсора Создателя в [index.html](file:///c:/Users/svato/bestdog/index.html) и [script.js](file:///c:/Users/svato/bestdog/script.js).
- **Control & State**: Контейнер тумблера `#cursor-toggle-container` отображается в Мем-Хабе (меню «Эффекты и темы») только после покупки курсора в магазине (`paw_cursor`). Состояние чекбокса `#cursor-checkbox` и класса `creator-cursor-active` на `body` синхронизируется с пользовательской настройкой в `localStorage.getItem('creator_cursor_user_pref')` и сохраняется при перезагрузке страницы.
- **Styling**: Добавлены премиальные неоновые стили переключателя в [style.css](file:///c:/Users/svato/bestdog/style.css), идеально сочетающиеся с темной и фиолетовой неоновой темой Вершителя.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.20` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.19] - 2026-05-17
### Fixed
- **Gameplay (Easter Egg Logic Fix)**: Исправлен критический баг перекрытия событий при проверке породы кнопкой 🤖. Счетчик кликов для вызова огурца обернут в жесткую проверку: если достижение `ms_ogurets` уже разблокировано, подсчет кликов и анимация огурца на карточках полностью отключаются.
- **Robustness**: При первом получении достижения анимация огурца (`spawnPickleRick`) и нейро-анализ породы запускаются параллельно и одновременно, больше не блокируя открытие окна породы. Кнопка ручного повторного вызова в Мем-Хабе продолжает работать без изменений.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.19` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.18] - 2026-05-17
### Changed
- **UX (Cursor Cache Bust)**: Пути кастомного курсора в [style.css](file:///c:/Users/svato/bestdog/style.css) переведены на использование нового оптимизированного файла `hero.png` (вместо закэшированного `my-face-cursor.png`) с чистыми абсолютными и относительными путями. Это гарантирует мгновенный обход кэша на всех браузерах пользователей.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.18` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.17] - 2026-05-17
### Added
- **Gameplay (Creator Scan Trigger)**: Интегрировано автоматическое получение достижения Вершителя (`creator_face_achievement`) и переключение темы при сканировании породы карточки Создателя в [script.js](file:///c:/Users/svato/bestdog/script.js).
- **Automation**: При сканировании породы на любой карточке Создателя теперь срабатывает вызов `unlockAchievement('creator_face_achievement')`, который начисляет +50 косточек, запускает всплывающее уведомление, принудительно удаляет классы `dark-theme` / `light-theme` и активирует `.vershitel-theme` на `document.body` с красивым тостом «😈 Режим Вершителя активирован!».
- **Cache Bumping**: Ресурсные версии подняты до `5.0.17` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.16] - 2026-05-17
### Fixed
- **UI/UX (Bulletproof Cursor Fix)**: Исправлен конфликт каскада стилей кастомного курсора в [style.css](file:///c:/Users/svato/bestdog/style.css). Полностью удален универсальный селектор `*` (`body.creator-cursor-active * { cursor: ... }`), который перекрывал стили для интерактивных элементов.
- **Specification**: Разделены правила для кликабельных элементов (где применяется лицо Создателя с лапкой-фоллбэком `pointer`) и для общего фона (где применяется лицо с фоллбэком `default`).
- **Cache Bumping**: Ресурсные версии подняты до `5.0.16` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.15] - 2026-05-17
### Added
- **Release (Forced Sync & Deploy)**: Принудительный деплой и синхронизация всех обновлений Режима Вершителя, включая новые лица Создателя в массиве `CREATOR_PHOTOS` и неоновое оформление интерфейса.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.15` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.14] - 2026-05-17
### Added
- **Content (New Creator Photos)**: Массив `CREATOR_PHOTOS` в [script.js](file:///c:/Users/svato/bestdog/script.js) расширен 15 новыми изображениями лица Создателя, добавленными в папку `my_faces/` (включая серии `images.jpg` и `Без названия.jpg`). Теперь среди обычных собак в ленте будет появляться гораздо больше разнообразных портретов Создателя!
- **Cache Bumping**: Ресурсные версии подняты до `5.0.14` в [index.html](file:///c:/Users/svato/bestdog/index.html) для обновления кэша скриптов на клиентах.

## [5.0.13] - 2026-05-17
### Added
- **Aesthetics (Creator Theme Polish)**: Полностью адаптированы стили главных кнопок шапки («📊 Рейтинг», «🏆 Зал славы»), кнопки «Смотреть еще собак» (`.action-btn`) и всех круглых кнопок действий на карточках собак специально для темы Вершителя (`.vershitel-theme`).
- **Styling**: Кнопки в режиме Создателя теперь имеют изысканный глубокий фиолетовый полупрозрачный фон (`rgba(20, 0, 40, 0.8)`), неоновую рамку, размытие заднего плана (`backdrop-filter`) и нежное фиолетовое неоновое свечение. При наведении они красиво заливаются цветом, увеличивают силу свечения и плавно сдвигаются наверх.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.13` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.12] - 2026-05-17
### Fixed
- **UI/UX (Cursor Ghost Fix)**: Исправлен критический баг исчезающего курсора (эффект «призрака»). Свойства `cursor` для `.creator-cursor-active` в [style.css](file:///c:/Users/svato/bestdog/style.css) переписаны с использованием каскада из относительных (`../img/my-face-cursor.png`, `img/my-face-cursor.png`) и абсолютных (`/img/my-face-cursor.png`) путей.
- **Specification**: Добавлены обязательные ключевые слова стандартного системного курсора (`default !important` и `pointer !important`) в конец каждого списка CSS-значений. Без них браузеры полностью игнорируют свойство `cursor: url()`.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.12` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.11] - 2026-05-17
### Fixed
- **UI (Meme Hub Sync)**: Исправлен критический баг Мем-Хаба. Кнопка «Тема Огурца» (`#trigger-cucumber-btn` / `🥒`) теперь корректно синхронизируется со статусом разблокировки достижения `ms_ogurets`. 
- **Refactoring**: Избирательное прямое изменение `userProgress.unlocked` при сканировании Создателя заменено на стандартный метод `window.unlockAchievement('ms_ogurets')`, который автоматически запускает `save()`, `updateUI()` и синхронизацию доступных мемов (`window.checkUnlockedMemes()`).
- **Robustness**: В `checkUnlockedMemes` добавлена принудительная очистка классов и применение `display: inline-block` для всех разблокированных кнопок, а также консолидированы обработчики кликов для кнопок вызова огурца.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.11` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.10] - 2026-05-17
### Fixed
- **Robustness (Image Fallback System)**: Внедрена отказоустойчивая система автозамены битых изображений. В [script.js](file:///c:/Users/svato/bestdog/script.js) для всех изображений собак (`dog-card`, `king-img`, `king-photo`) в обработчиках `onerror` реализована мгновенная подмена источника на стабильный, высокодоступный CDN Unsplash (`https://images.unsplash.com/photo-1543466835-00a7907e9de1?q=80&w=300&auto=format&fit=crop`) при сбоях сети (`ERR_CONNECTION_RESET`) от API Dog CEO.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.10` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.9] - 2026-05-17
### Fixed
- **Network Stability (Fetch Batch Limiting)**: Исправлена критическая ошибка `ERR_CONNECTION_RESET` (сброс соединений сервером). В функции `loadDogsBatch` в [script.js](file:///c:/Users/svato/bestdog/script.js) жестко зафиксирован лимит загрузки: строго по **9** изображений собак за один цикл. Блокирующий флаг `isFetchingDogs` гарантирует, что следующая порция не будет запрашиваться до завершения текущей.
- **Path Fix (Absolute Cursor Path)**: В [style.css](file:///c:/Users/svato/bestdog/style.css) для правила `.creator-cursor-active` изменен путь к файлу курсора на абсолютный от корня: `/img/my-face-cursor.png`. Это предотвращает ошибки относительного поиска ресурсов на вложенных страницах или при маршрутизации.
- **Cache Bumping**: Ресурсные версии подняты до `5.0.9` в [index.html](file:///c:/Users/svato/bestdog/index.html).

## [5.0.8] - 2026-05-17
### Added
- **Feature: Exclusive Creator Cursor v1.1.0**: Реализована замена стандартного курсора лапки на эксклюзивное фото Создателя (`img/my-face-cursor.png`) с прозрачным фоном и выверенной точкой клика (`16 16`).
- **Logic**: При покупке «кастомного курсора» в магазине на тело документа теперь вешается класс `.creator-cursor-active`, и статус активности сохраняется в `localStorage` (`creator_cursor_enabled`).
- **Cache Bumping**: Версии ресурсов повышены до `5.0.8` для гарантированного сброса кэша браузера.

## [5.0.7] - 2026-05-17
### Fixed
- **Cursor Recovery (Cursor Fix)**: Исправлен критический баг с исчезающим курсором. Полностью удалено ошибочное свойство `cursor: url(...)` с пустым SVG-изображением для `body.paw-cursor-active`.
- **Hard-coded Stability**: Внедрено безусловное глобальное правило в CSS, принудительно возвращающее стандартный указатель мыши (`cursor: pointer !important`) на все кнопки, ссылки, карточки собак и интерактивные элементы, а также стандартный курсор (`cursor: default !important`) для `body` и `html`.

## [5.0.6] - 2026-05-17
### Fixed
- **King Modal Redesign**: Внедрен премиальный «королевский» дизайн окна Короля Недели с золотым свечением и темным фоном.
- **Bug Fix**: Исправлен баг отображения рейтинга в окне Короля (убраны лишние символы `:-`).

## [5.0.5] - 2026-05-17
### Fixed
- **Strict Content Restoration**: Весь текст и эмодзи переписаны строго по проектному словарю.
- **UI**: Плашка на карточках заменена на «Новая ✨».
- **UI**: Иконки взаимодействий: ❤️ (Лайк), ⭐️ (Оценка), 🤖 (Нейро-анализ).
- **Navigation**: Реорганизовано правое меню (🥇, 🦴, 👤, 🧩).
- **Header**: Кнопки в шапке: 📊 Рейтинг и 🏆 Зал славы.

## [5.0.4] - 2026-05-17
### Fixed
- **UI Restoration**: Восстановлены CSS-переменные в `:root`.
- **UI**: Исправлены «кнопки-призраки» (белый текст на белом фоне) в светлой теме.
- **Testing**: Исправлены стили тестовой кнопки (padding, контрастность).

## [5.0.3] - 2026-05-17
### Fixed
- **Content Restoration**: Полное восстановление русского текста и эмодзи в `index.html` и `script.js`.
- **UI**: Вернулись иконки на карточки (❤️, ⭐️, 🧬) и в модальные окна.
- **Testing**: Восстановлена тестовая кнопка «😎 ТЕСТ РЕЖИМА» с корректной JS-логикой.

## [5.0.2] - 2026-05-17
### Fixed
- **Emergency Rollback & ASCII Cleanup**: Все не-ASCII символы (кириллица и невидимый мусор) удалены из `style.css`, `script.js` и `index.html` для восстановления работоспособности парсеров браузера.
- **UI Restoration**: Восстановлены стили зеленой кнопки `#load-more-btn` и темы `.vershitel-theme` (без кириллических комментариев).
- **Stability**: Сайт снова корректно отображает стили. Версия поднята до 5.0.2.

## [5.0.1] - 2026-05-17
### Fixed
- **Critical: Global Encoding Repair**: Исправлена массовая порча кодировки (Mojibake) во всех файлах проекта (`index.html`, `style.css`, `script.js`). Все русские комментарии и тексты восстановлены.
- **Cleanup**: Удалены дубликаты и "мусорные" символы в конце `style.css`.
- **Consistency**: Проверена работоспособность всех тем и тестовой кнопки.

## [5.0.0] - 2026-05-17
### Added
- **Feature: Creator Mode (Vershitel) v5.0.0 TEST**: Глобальное обновление интерфейса.
- **Testing**: Добавлена временная кнопка «😎 ТЕСТ РЕЖИМА» для мгновенного переключения темы разработчика.
- **Visuals**: Внедрена текстура лица Создателя (`img/face-texture.png`) в качестве плиточного фона.
- **UI**: Полупрозрачные карточки, модальные окна и сайдбары с эффектом `backdrop-filter: blur(5px)`.
- **Neon Styles**: Добавлены неоновые рамки и свечение текста для режима ВЕРШИТЕЛЯ.

## [4.4.0] - 2026-05-17
### Added
- **Feature: Creator Mode (Vershitel)**: Легендарная третья тема сайта! При нахождении и открытии фото разработчика в полный экран разблокируется секретный режим.
- **Achievement**: Добавлена ачивка «Познал Лицо Создателя» 😎.
- **Meme Hub**: В меню эффектов добавлена кнопка переключения на тему Создателя (доступна только после получения ачивки).
- **Visuals**: Тема Создателя полностью меняет фон сайта на текстуру лица разработчика с неоновыми рамками и Glassmorphism-эффектами.

## [4.3.9] - 2026-05-17
### Changed
- **UX: Pagination Overhaul**: Полностью переписана логика подгрузки. Удалены все фоновые лоадеры и автоматика.
- **Ironclad Logic**: Внедрен механизм «железобетонного сброса» кнопки в блоке `finally`. Теперь кнопка гарантированно разблокируется даже при сбоях сети или API.
- **Visuals**: Кнопка «Смотреть ещё собак 🐾» теперь использует класс `.action-btn` и визуально отображает статус загрузки (изменение прозрачности и курсора).
- **Cleanup**: Удалены устаревшие элементы `#sync-loader` и `.sync-text` через CSS.

## [4.3.8] - 2026-05-17
### Fixed
- **Critical: ReferenceError Fix**: Исправлена фатальная ошибка `loadMore is not defined` в функции `loadDogsBatch`. Теперь первичная загрузка и ручное управление работают корректно.
- **UI Restoration**: Кнопке «Смотреть ещё собак 🐾» возвращен класс `action-btn` и добавлены центрирующие стили.
- **Cache Bumping**: Версия обновлена до 4.3.8 для сброса кэша.

## [4.3.7] - 2026-05-17
### Changed
- **UX: Manual Loading**: Бесконечный скролл отключен по просьбе пользователя. Возвращена кнопка «Смотреть ещё собак 🐾» для ручного контроля подгрузки контента.
- **Logic**: Добавлен механизм блокировки кнопки (Throttle) во время запроса к API, чтобы предотвратить дублирование запросов. Кнопка меняет текст на «Синхронизация... ⏳» в процессе загрузки.
- **CSS**: Улучшены отступы и центровка контейнера `.load-more-container`.

## [4.3.6] - 2026-05-17
### Fixed
- **CSS Leak Fix (Final)**: Жесткая изоляция стилей мини-игры. Ограничение `max-height: 180px` теперь применяется строго только внутри `.game-modal`.
- **Gallery Restoration**: Возвращены полноразмерные квадратные карточки (`aspect-ratio: 1/1`) с качественным заполнением (`object-fit: cover`).
- **UI: New Badge (Premium)**: Плашка «Новая ✨» переименована в `.new-badge`, получила премиальный золотой фон, эффект матового стекла (backdrop-filter) и абсолютное позиционирование, исключающее Layout Shift.
- **Stability**: Исправлены конфликты `transform: translateZ(0)` и `position: absolute`.

## [4.3.5] - 2026-05-17
### Changed
- **UX Revert: Manual Loading**: Отключен бесконечный скролл. Новые порции собак теперь подгружаются только при нажатии на кнопку «Загрузить еще пёсиков 🐕».
- **Optimization**: Удален `IntersectionObserver` и сопутствующая логика автоматической подгрузки для снижения нагрузки на браузер.

## [4.3.4] - 2026-05-17

### Fixed
- **Logic: Infinite Loader Fix**: Исправлен баг «бесконечного лоадера». Флаг загрузки теперь гарантированно сбрасывается в блоке `finally`.
- **Feature: End of Content**: Добавлена обработка завершения контента. Если API возвращает пустой список, кнопка меняет текст на «Вы посмотрели всех пёсиков! 🐾», а автоматическая подгрузка отключается.
- **Stability**: Улучшена обработка ошибок сети при подгрузке новых порций изображений.

## [4.3.3] - 2026-05-17

### Added
- **UI Micro-interactions**: Добавлен эффект увеличения (`scale(1.1)`) при наведении на плашку баланса косточек в шапке. Теперь взаимодействие с балансом ощущается так же отзывчиво, как и с другими кнопками интерфейса.
- **UX**: Улучшена визуальная обратная связь для интерактивных элементов.

## [4.3.2] - 2026-05-17

### Fixed
- **CSS Hotfix: Gallery Restoration**: Исправлена ошибка, из-за которой стили мини-игры ограничивали размер фото в главной галерее. Фотки снова крупные и сочные!
- **UI: New Badge Fix**: Плашка «Новая ✨» теперь корректно позиционируется поверх карточек с эффектом Glassmorphism и не вызывает смещения верстки.
- **Style Isolation**: Стили мини-игры теперь строго изолированы от глобальных стилей галереи.

## [4.3.1] - 2026-05-17

### Fixed
- **Hotfix: Syntax Error**: Исправлена критическая ошибка `Uncaught SyntaxError` (лишняя фигурная скобка), которая приводила к полной остановке выполнения скрипта.
- **Cache Bumping**: Версия обновлена до 4.3.1.

## [4.3.0] - 2026-05-17

### Fixed
- **Logic: New Balance Trigger Separation**: Исправлена логика активации пасхалки.
    - Клик по балансу в шапке теперь открывает модальное окно баланса без запуска анимации (если ачивка уже получена).
    - Кнопка в Мем-Хабе 👟 теперь запускает анимацию при каждом нажатии, независимо от статуса ачивки.
- **UX**: Улучшено поведение интерфейса при просмотре сбережений.

## [4.2.9] - 2026-05-17

### Added
- **Performance: Infinite Scroll & Batching**: Реализована ленивая подгрузка контента через `IntersectionObserver`. Теперь новые пёсики подгружаются автоматически при скролле.
- **Stability: Network Throttling**: Внедрена искусственная задержка (150мс) при рендеринге карточек в пачке. Это снижает пиковую нагрузку на сеть и предотвращает ошибки `net::ERR_CONNECTION_RESET`.
- **Reliability: Advanced Image Fallback**: Улучшена обработка битых изображений. При ошибке загрузки теперь подставляется SVG-заглушка и применяется визуальный фильтр `.image-failed`.
- **Optimization**: Атрибуты `loading="lazy"` и `decoding="async"` теперь гарантированно применяются ко всем изображениям галереи.

## [4.2.8] - 2026-05-17

### Changed
- **Animation Polish**: Скорость бега кроссовка Нью Беленс 👟 снижена до 3.5 секунд. Теперь он пролетает через экран плавно и вальяжно (ease-in-out).
- **Bugfix**: Таймер удаления элемента синхронизирован с новой длительностью анимации.

## [4.2.7] - 2026-05-17

### Changed
- **UI: Meme Hub (Panel of Effects)**: Рефакторинг боковой панели. Все секретные триггеры и пасхалки объединены в единый разворачивающийся хаб ✨.
    - Кнопки Огурчика 🥒 и Кроссовка 👟 теперь живут внутри хаба и появляются только после разблокировки достижений.
    - Реализовано плавное выезжающее подменю с Glassmorphism-эффектом.
- **Optimization**: Централизованная логика проверки разблокированного контента `checkUnlockedMemes`.
- **Cache Bumping**: Версия обновлена до 4.2.7.

## [4.2.6] - 2026-05-17

### Added
- **Easter Egg: New Balance**: Реализована легендарная пасхалка при клике на баланс косточек.
    - Динамическая анимация летящего кроссовка `nb-sneaker.png` через весь экран.
    - Компактное модальное окно с детальной информацией о текущем финансовом состоянии.
    - Секретное достижение «МС Нью Беленс» (+50 косточек).
- **Cache Bumping**: Версия обновлена до 4.2.6 для активации новых стилей и логики.

## [4.2.5] - 2026-05-17

### Fixed
- **Mini-Game UI Compactness**: Решена проблема вертикального переполнения модального окна. Высота изображения ограничена до 180px, сокращены отступы у заголовка, плашки наград и сетки ответов. Теперь интерфейс игры полностью помещается в экран без необходимости скролла.

## [4.2.4] - 2026-05-17
### Fixed
- **Cursor Calibration**: Точка клика (hotspot) кастомного курсора лапки установлена на `16 16`. Теперь прицел на кнопках и крестиках идеально точный.

## [4.2.3] - 2026-05-17
### Changed
- **Universal Cursor**: Кастомный курсор теперь принудительно применяется ко всем интерактивным элементам (кнопки, ссылки, виджеты), не переключаясь на системный `pointer`.

## [4.2.2] - 2026-05-17
### Fixed
- **Dark Theme Glow**: Убрано избыточное «выжигающее» свечение иконок в темной теме. Заменено на деликатный `text-shadow` только для тусклых эмодзи (лапки, джойстик).

## [4.2.1] - 2026-05-17
### Fixed
- **Scroll Lock**: Реализована полная блокировка фонового скролла при открытых модальных окнах (класс `no-scroll`).
- **Modal Borders**: Добавлена контрастная обводка `2px` для всех модальных окон в светлой теме для улучшения структуры интерфейса.

## [4.2.0] - 2026-05-17
### Fixed
- **Lightbox Physics**: Полностью переписана логика зума. Теперь только скролл управляет масштабом, добавлено ограничение (Clamp) для перетаскивания (Drag-to-Pan), чтобы картинка не улетала за экран.
- **Lightbox Content**: Исправлено отображение маленьких фото — теперь они адаптивно заполняют окно и не обрезаются контейнером.

## [4.1.9] - 2026-05-17
### Changed
- **King Modal Redesign**: Масштабное обновление модального окна «Король недели» в светлой теме: премиальный белый фон, глубокие тени, золотая рамка для фото и глянцевый бейдж лайков.
- **Cache Bumping**: Мажорное обновление версии для сброса кэша стилей и скриптов.

## [4.1.8] - 2026-05-16
### Fixed
- **Lightbox Auto-Zoom**: Удалена проблемная логика автоматического приближения при наведении мыши. Теперь зум работает только от колесика.
- **Lightbox Pan**: Добавлена возможность перетаскивания (drag-to-pan) изображения в Лайтбоксе при увеличении.

## [4.1.7] - 2026-05-16
### Added
- **Dynamic JS Fixes**: Внедрена функция `fixVisualBugsDynamically` для принудительного удаления границ и теней у виджетов через DOM, обходя кэширование CSS.

## [4.1.6] — 2026-05-16
### 📱 Mobile UI: Action Buttons & Tooltip Fix
- **Hard Alignment**: Зафиксирована жёсткая центровка (flex-column) внутри круглых кнопок действий. Эмодзи и цифры больше не вылетают за границы круга.
- **Tooltip Cleanup**: На смартфонах теперь автоматически удаляются атрибуты `title`, что предотвращает появление раздражающих системных подсказок при нажатии на кнопки.
- **Tap Feedback**: Убрана стандартная синяя подсветка при нажатии (`tap-highlight`) на мобильных устройствах.

## [4.1.5] — 2026-05-16
### 🎨 UI: Light Theme Visual Polish
- **Dock Adaptation**: Нижний док-бар в светлой теме теперь имеет полупрозрачный белый фон («матовое стекло») с мягкой тенью.
- **Card Controls**: Боковые кнопки действий на карточках собак адаптированы под светлый режим: светлый фон, темные иконки и контрастные границы.
- **Game UX**: Кнопки вариантов ответов в мини-игре получили четкую обводку и контрастный текст для лучшей читаемости в светлом режиме.

## [4.1.4] — 2026-05-16
### 🛡️ Navigation: Secure Hall of Fame Exit
- **Asymmetric Fix**: Исправлена критическая ошибка асимметрии истории. Теперь открытие Зала славы гарантированно создает уникальный шаг в истории (`type: hallOfFame`).
- **Safe Back-Check**: Кнопка закрытия (крестик) теперь проверяет наличие маркера `modalOpen` в текущем состоянии `history.state` перед вызовом `history.back()`. Это предотвращает случайный выход с сайта.
- **Fail-safe Logic**: Добавлен запасной механизм закрытия UI, если шаг в истории по какой-то причине отсутствует.

## [4.1.3] — 2026-05-16
### 📱 Mobile UX: Viewport & Visibility Final Fix
- **Enhanced Viewport**: Обновлен мета-тег `viewport` для принудительного масштаба 1:1 и запрета системного зума. Это гарантирует, что браузер применит `@media` правила для мобильных разрешений.
- **Strict Visibility**: Добавлены свойства `visibility: visible` и `opacity: 1` для док-бара, чтобы исключить его скрытие системными стилями или предыдущими правилами.
- **Debug Indicator**: В мобильную версию добавлено свечение логотипа (`text-shadow`) в качестве индикатора успешного срабатывания медиа-запросов.

## [4.1.2] — 2026-05-16
### 🔍 Mini Game: UI Fix & Image Zoom
- **Perfect Alignment**: Исправлено выравнивание фотографии в мини-игре. Теперь изображение центрируется без обрезки (`object-fit: contain`) в фиксированном контейнере.
- **Click to Zoom**: Добавлена функция детального просмотра. При клике на фото собаки в игре открывается полноэкранный "Glassmorphism" оверлей с увеличенным изображением.
- **Interaction**: Внедрен плавный переход и стильный курсор `zoom-in` для улучшения UX.

## [4.1.1] — 2026-05-16
### 🚀 Performance: GPU Rendering Optimization
- **GPU Acceleration**: Все карточки собак (`.dog-card`) переведены на аппаратное ускорение через `translate3d(0,0,0)` и `will-change`.
- **Render Isolation**: Добавлено свойство `contain: paint` для карточек, что предотвращает перерисовку всей страницы при изменении состояния отдельной карточки.
- **Blur Optimization**: Кнопки и плашки с `backdrop-filter` теперь имеют собственные композитные слои (`translateZ(0)`).
- **Animation Refactoring**: Анимация Огурчика Рика переписана на `transform: translate3d`.

## [4.1.0] — 2026-05-16
### 🕹️ Mini Game: Guess the Breed (Restored)
- **Gameplay**: Реализована полноценная мини-игра "Угадай породу". 4 варианты ответа, система очков (+100) и комбо-серий.
- **Progression**: Интеграция с системой достижений. За серию из 5 верных ответов выдается ачивка "Кинолог-эксперт".

## [4.0.9] — 2026-05-16
### ✨ Smarter Badges: Seen Dogs System
- **Persistence**: Реализована система отслеживания просмотренных собак через `seen_dog_ids` в `localStorage`.
- **Dynamic Badges**: Плашка "Новая ✨" теперь отображается только при первой встрече с фотографией.

## [4.0.8] — 2026-05-16
### 🛠️ Firebase Fix & UI Polish
- **Firebase CDN Restore**: Возврат к стабильным ES-импортам через официальный CDN (v10.11.1).
- **Сетка 3х3 (9 собак)**: Восстановлен объем подгрузки.
- **Xbox-style Достижения**: Реализована визуальная часть и уведомления.
- **Идеальное выравнивание**: Flexbox для заголовков модальных окон.

## [4.0.7] — 2026-05-16
### 🔥 Firebase Production (Real-time Multiplayer)
- **Firestore SDK**: Локальные заглушки (`./js/firebase-app.js`) полностью заменены на официальный Firebase 10 CDN (`gstatic.com`). `script.js` теперь загружается как `type="module"`, что необходимо для ES-импортов.
- **Реальный `syncLike`**: Функция `toggleLike` теперь вызывает `window.syncLike(url, isLike)`, которая использует `getDoc` + `setDoc`/`updateDoc(increment(±1))` для атомарного обновления поля `likes` в Firestore.
- **Реальный `syncVote`**: При сохранении вердикта вызывается `window.syncVote(url, scoreData)`. Функция сохраняет плоские данные оценки (`cringe`, `cute`, `aggro` и т.д.) через `arrayUnion` и пересчитывает средние баллы (`avg`, `avg_cringe`...) прямо в Firestore документе.
- **Real-time Рейтинг (`onSnapshot`)**: Вкладка «Общий топ» в модальном окне рейтинга подписана на `onSnapshot` коллекции `dogs`. Данные сортируются по выбранному критерию прямо на стороне Firestore (`orderBy`). Любое изменение в БД (от любого пользователя в мире) мгновенно перерисовывает Топ-10.
- **Умный lifecycle**: При открытии модалки рейтинга — подписка активируется. При закрытии и при переключении на вкладку «Мои топы» — подписка корректно отменяется (`_unsubscribeGlobal()`), предотвращая утечки памяти.
- **Оффлайн-режим**: Если Firebase не настроен (пустые ключи) — проект продолжает работать в локальном режиме с `console.warn`.

## [4.0.6] — 2026-05-16
### Магазин Кастомизации и Достижения (Gamification Update)
- **Иконки товаров**: В Магазине кастомизации дефолтные эмодзи 🎁 заменены на уникальные иконки для каждого товара (🐾, 🥇, 🔥).
- **Визуальные эффекты магазина**: 
  - Покупка «Собачьей лапки» глобально меняет курсор на сайте на лапку 🐾. Состояние сохраняется после перезагрузки.
  - Покупка «Золотого бейджа» превращает скучные бейджи рейтинга на карточках в сияющие золотые 🥇.
- **Интеграция Супер-лайка**: Расходник «Супер-лайк» теперь дает ровно +5 бонусных голосов выбранной собаке (с мок-отправкой в базу данных).
- **Система достижений**: Восстановлена базовая логика ачивок из v2.8.0. За выполнение условий (оценено 10 собак, угадано 5 пород, поставлено 20 лайков) выдается бонус +50 косточек и всплывает Toast-уведомление.

## [4.0.5] — 2026-05-16
### Рефакторинг Рейтингов и Идеальная Сетка (Data Aggregation Update)
- **Идеальная сетка**: Кнопка «Загрузить ещё» теперь запрашивает ровно по 3 собаки, сохраняя симметрию колонок. Добавлена блокировка от спам-кликов и состояние загрузки.
- **Локальный Топ Пёсиков**: Старая система колонок в модальном окне Рейтинга заменена на удобные вкладки (Tabs). Написана новая функция `renderRatings`, которая динамически сканирует галерею и собирает два актуальных Топ-10: «По баллам» и «По лайкам».
- **Зал Славы (Битва пород)**: С нуля написан алгоритм агрегации данных. Скрипт собирает все оценки собак, вычисляет средний балл для каждой породы и генерирует наградной HTML-пьедестал для тройки лидеров.
- **Косметика бейджей**: Иконка на плашке неоцененных собак заменена с лапок на магические искры (`Новая ✨`).

## [4.0.4] — 2026-05-16
### Ультра-премиум UI и Логика (Ultra-Premium Update)
- **Честные лайки**: Полностью удалена фейковая генерация лайков для новых карточек (теперь все пёсики честно начинают с 0).
- **Свобода Вердикта**: Реализована возможность изменения оценки. Кнопка голосования динамически меняет текст на «Изменить вердикт ♻️». Переголосование обновляет UI на карточке, но не позволяет фармить (абузить) косточки.
- **Интерактивный зум в Вердикте**: Фотографию в окне оценки теперь можно кликнуть для увеличения через полноэкранный Лайтбокс.
- **Пригвождённый баланс**: Счетчик косточек (`.bones-wallet`) закреплен (fixed) в правом верхнем углу с применением эффекта Glassmorphism, чтобы всегда оставаться на виду и не перекрывать переключатель темы.
- **Flexbox-магия ползунков**: Верстка окна оценивания доведена до идеала. Цифры значений выровнены по центру в один ряд со слайдерами и получили фиксированную ширину, предотвращая скачки интерфейса.

## [4.0.3] — 2026-05-16
### Глобальное восстановление механик и UI (Post-Rollback Fixes)
- **Система Вердикта**: Полностью восстановлена логика окна оценивания. Скрипт мгновенно вычисляет средний балл с ползунков, закрывает модальное окно, обновляет локальное хранилище и выводит бейдж `⭐ [балл]` прямо на карточку. Защита от дублей: одной собаке — один голос.
- **Механика лайков**: При постановке лайка страница больше не перезагружается. Добавлен локальный счетчик лайков на карточки. Исправлен топорный красный фон кнопки — теперь сердце плавно увеличивается и отбрасывает мягкое свечение.
- **Определение породы (ИИ Сканер)**: Починена выезжающая матовая панель (`.breed-info-panel`) под фото собаки.
- **Зал Славы**: В HTML восстановлена структура вкладок («Номинанты недели» и «Битва пород»), возвращена логика переключения между ними.
- **UI & Косметика**: В CSS добавлена Flexbox-правила для идеального вертикального выравнивания всех эмодзи с текстом по всему проекту.

## [4.0.2] — 2026-05-16
### Restoration Protocol
- Восстановлен случайно удаленный виджет «Король недели» (King of the Week) в левом сайдбаре.
- Исправлены обработчики событий (клики) для всех кнопок боковых панелей.
- Добавлена защищенная инициализация модальных окон с использованием Optional Chaining.
- Оптимизирована функция `renderKingBanner` для отображения актуальных данных.

---

## [4.0.1] — 2026-05-16
### Hotfix: White Screen & HTML Corruption
- Исправлена критическая ошибка парсинга HTML (незакрытые теги и «утекший» код), приводившая к белому экрану.
- Проведен полный рефакторинг и очистка `index.html`.
- В `script.js` добавлена защищенная инициализация через `DOMContentLoaded`.
- Внедрены проверки на существование DOM-элементов (null-checks), что предотвращает фатальные падения скрипта при отсутствии элементов.

---

## [4.0.0] — 2026-05-16
### Фикс контрастности Зала Славы в Светлой теме
- Исправлен баг читаемости в Зале Славы для светлой темы. 
- Адаптированы цвета текста (лайки стали темно-бирюзовыми) и пьедесталы для корректного отображения на светлом фоне.
- Имена и позиции в списке получили высококонтрастный темный цвет.

---

## [3.9.9] — 2026-05-16
### Ультра-оптимизация производительности (Smooth Scroll)
- Внедрено асинхронное декодирование изображений (decoding="async") для устранения микрофризов при загрузке.
- Реализован хак с временным отключением `pointer-events` во время прокрутки для снижения нагрузки на CPU и повышения FPS.
- Добавлена GPU-изоляция карточек через `translateZ(0)`.

---

## [3.9.8] — 2026-05-16
### Фикс UI и путей (Светлая тема)
- Исправлена маршрутизация статических файлов для ассета Огурчика Рика (добавлен абсолютный путь).
- Реализована полная поддержка светлой темы для UI-компонентов экономики (белый Glassmorphism, контрастная обводка иконок).

---

## [3.9.7] — 2026-05-16
### Экономическое обновление (Хотфикс UI)
- Премиальный редизайн виджета баланса косточек (добавлен темный Glassmorphism и Teal-обводка).

---

## [3.9.6] — 2026-05-16
### Оптимизация (Операция «Анти-Лаг»)
- Внедрена технология `content-visibility: auto` для карточек собак. Это значительно снижает нагрузку на CPU/GPU при длинном скролле, так как браузер не рендерит карточки вне зоны видимости.
- Добавлен `contain-intrinsic-size` для предотвращения скачков прокрутки при ленивой отрисовке.
- Исправлена прозрачность Огурчика Рика — теперь он стал ярким и заметным (увеличена непрозрачность в анимации).

---

## [3.9.5] — 2026-05-16
### Экономическое обновление «Косточки» 🦴
- Внедрена игровая валюта «Косточки» (🦴).
- Запущен **Магазин кастомизации** с премиальными товарами:
    - **🐾 Собачья лапка**: Кастомный курсор на всем сайте.
    - **✨ Золотой бейдж**: Эффектный золотой градиент для рейтинга собак.
    - **🔥 Супер-лайк**: Расходный материал для мощного буста лайков любой собаке (+5 за раз).
- Система вознаграждений: +1 🦴 за оценку собаки, +5 🦴 за победу в раунде мини-игры.
- Полная интеграция с `localStorage` для сохранения баланса и покупок.

---

## [3.9.4] — 2026-05-16
### Откат UI и Редизайн карточек галереи (Floating Actions)
- **Откат Вердикта**: Удален эффект Glassmorphism с правой панели окна оценки, возвращен стандартный непрозрачный фон.
- **Редизайн карточек собак**: Фотографии теперь занимают всю площадь карточки.
- **Плавающая панель**: Боковая панель действий превращена в абсолютный «стеклянный» (Glassmorphism) виджет поверх изображения.
- **Улучшение читаемости**: Добавлены тени (drop-shadow) и иконкам и цифрам для лучшей контрастности на фоне любых изображений.

---

## [3.9.3] — 2026-05-16
### Премиальный UI-редизайн окна Вердикта
- Фон правой панели заменен на эффект Glassmorphism (матовое стекло) с размытием заднего плана.
- Полоски оценки интегрированы с неоновым свечением и легкой прозрачностью для лучшего визуального баланса.

---

## [3.9.2] — 2026-05-16
### Багфикс (Динамические рейтинги)
- Исправлен баг отображения рейтинга. Бейджи на карточках теперь динамически подтягивают средний балл из хранилища, а статус «Новая» отображается только для неоцененных собак.

---

## [3.9.1] — 2026-05-16
### Исправлено
- **UI Мини-игры**: Исправлено наслоение кнопки закрытия на текст рекорда (Лучший).
- **Функциональность**: Добавлена возможность клика по фотографии в игре для открытия в полноэкранном режиме (Lightbox).
- **UI-полировка карточек**: Удалены громоздкие шкалы оценок с главных фотографий. Добавлен компактный Glassmorphism-бейдж с общим средним баллом (⭐).

---

## [3.9.0] — 2026-05-16
### Добавлено
- **Мини-игра «Угадай породу»**: Интерактивный режим викторины на время.
- Добавлена новая кнопка в правой панели (🕹️) для быстрого доступа к мини-игре.
- Интеграция с системой достижений: добавлена новая ачивка «Кинолог-эксперт 🎯» (5 очков подряд).
- Система комбо-очков, таймер и сохранение локальных рекордов (`Best Score`).

---

## [3.8.9] — 2026-05-16
### Геймификация пасхалки (Pickle Rick Summoner)
- Добавлена секретная кнопка «Вызвать Рика» (🥒) в левый сайдбар.
- **Логика разблокировки**: Кнопка скрыта по умолчанию и навсегда появляется только после нахождения пасхалки Создателя (получение ачивки «МС ОГУРЕЦ»).
- **Мгновенная активация**: При нахождении пасхалки кнопка эффектно появляется с анимацией scale-in.
- **Безлимитный спавн**: Кнопка позволяет призывать неограниченное количество бегущих Огурчиков Риков (каждый клик — новый Рик!).

---

## [3.8.8] — 2026-05-16
### Багфикс (Pickle Rick)
- Устранен баг двойного рендера текстового облачка у пасхалки Огурчика Рика (удален конфликтующий CSS-псевдоэлемент).

---

## [3.8.7] — 2026-05-16
### Фикс путей (Pickle Rick)
- Исправлен путь к файлу Огурчика Рика (убрана несуществующая подпапка animations, путь направлен напрямую в img/).

---

## [3.8.6] — 2026-05-16
### Секретный патч «Беглец» (Хотфикс)
- Исправлен сломанный путь к ассету Огурчика Рика и откалибровано позиционирование текстового облачка.

---

## [3.8.5] — 2026-05-16
### Секретный патч «Беглец»
- Реализована полноэкранная хардверная CSS-анимация пробега Огурчика Рика (Pickle Rick Marathon) при получении секретной ачивки с использованием локального графического ассета.

---

## [3.8.4] — 2026-05-16
### Исправлено (Беглец v2)
- Исправлена проблема с загрузкой изображения Огурчика Рика (замена URL на более стабильный + fallback на эмодзи).
- Добавлены логи в консоль для отслеживания детекции создателя и активации ачивки.
- Добавлена команда `window.resetAchievements()` в консоль для сброса прогресса при тестировании.

---

## [3.8.3] — 2026-05-16
### Секретный патч «Беглец»
- Вместо всплывающей плашки, Огурчик Рик теперь физически пробегает через весь экран от левого до правого края при получении ачивки «МС ОГУРЕЦ».
- Реализована аппаратная CSS-анимация с текстовым облачком («WUBBA LUBBA DUB DUB!») и эффектами раскачивания.

---

## [3.8.2] — 2026-05-16
### Добавлено
- **Система достижений (Gamification)**: Внедрена полноценная система ачивок для повышения вовлеченности.
- Добавлена статистика пользователя (количество оценок, определений пород).
- **Уведомления**: Новые всплывающие уведомления в стиле Xbox при разблокировке достижений.
- **Кабинет достижений**: Модальное окно со списком всех наград, включая секретные.
- Первые 4 ачивки: «Кинолог», «Собачий сканер», «Добряк» и секретная «Кто здесь Босс?».

---

## [3.8.1] — 2026-05-16
### Изменено
- **Титулы Создателя**: Шуточная порода для фотографий разработчика теперь выбирается случайным образом из списка: банник, лысина, гений миллиардер филантроп, МС СВО, МС ОГУРЕЦ.
- Реализована привязка титула к конкретному фото для консистентности отображения.

---

## [3.8.0] — 2026-05-16
### Исправлено
- **The Creator's Return**: Исправлены пути к фотографиям разработчика (перенос в `my_faces/`).
- Усовершенствована логика вставки пасхалок: теперь они гарантированно появляются на 18-м, 36-м и т.д. местах.
- Добавлены отладочные логи в консоль для контроля внедрения пасхалок.
- Уточнено шуточное название породы: «Homo Sapiens (Разработчик)».

---

## [3.7.9] — 2026-05-16
### Добавлено
- **Умная полупрозрачность (Fade-on-Hover)**: Реализована система динамической прозрачности для боковых панелей. Виджеты теперь имеют `opacity: 0.4` in покое и `opacity: 1` при наведении.
- Улучшены ховер-эффекты: добавлено увеличение `scale(1.08)` и мягкое неоновое свечение Teal.

---

## [3.7.8] — 2026-05-16
### Исправлено (Emergency Patch)
- **Пропавшие собаки**: Добавлен `onerror` fallback для картинок, чтобы при недоступности локального фото или сбое загрузки отображался запасной пёсик. Также добавлено логирование в консоль.

---

## [3.7.7] — 2026-05-16
### Добавлено
- **Фото создателя**: Добавлен скрытый механизм генерации пасхалок (инъекция фото разработчика в общую сетку с частотой 1:18), реализована шуточная система распознавания для недокументированных видов.

---

## [3.7.6] — 2026-05-16
### Добавлено
- **Симметрия интерфейса**: Добавлен левый боковой виджет «Общий рейтинг» (📊) под виджетом Короля.
- Улучшено выравнивание боковых панелей для достижения визуального баланса.

---

## [3.7.5] — 2026-05-16
### Изменено
- **Золотое кольцо (Ring of Gold)**: Визуальное обновление виджета Короля. Иконка короны заменена на премиальную градиентную обводку с эффектом жидкого золота.
- Добавлено мягкое мистическое свечение Teal вокруг золотого кольца.

---

## [3.7.4] — 2026-05-16
### Изменено
- **Pixel-Perfect Sidebars**: Редизайн боковых виджетов в компактном круглом стиле (Pixel-Perfect Sidebars), внедрение всплывающих подсказок (Tooltips).
- Глобальное скрытие скроллбаров на боковых панелях.

---

## [3.7.3] — 2026-05-16
### Добавлено
- **Sticky Sidebars**: Добавлены фиксированные боковые панели (Sticky Sidebars) для эффективного использования пространства на ультрашироких мониторах.

---

## [3.7.2] — 2026-05-16
### Добавлено
- **Битва пород**: Добавлена глобальная статистика по породам («Битва пород»), реализована система вкладок в Зале славы.

---

## [3.7.1] — 2026-05-16
### Добавлено
- **Король недели**: Добавлен баннер Короля недели на главную страницу.
- **Зал славы**: Добавлена модалка с номинациями.

### Исправлено
- **Z-index layering**: Исправлен баг, при котором полноэкранное фото открывалось за окном рейтинга. Теперь Лайтбокс имеет высший приоритет (`z-index: 9999`).
- **Фокус окон**: Окно рейтинга теперь остается открытым при просмотре фото, но корректно перекрывается затемнением Лайтбокса.

---


## [3.7.0] — 2026-05-16
### Добавлено
- **Полная автономность (Resilience)**: Все внешние библиотеки (Firebase SDK, Vanilla-tilt) и шрифты (Outfit) перенесены в локальные папки `js/` и `fonts/`. Проект полностью независим от внешних CDN и VPN.
- **Офлайн-режим**: Внедрена система автоматического переключения на `localStorage` при недоступности Firebase. Добавлены локальные фото-заглушки для работы без интернета.
  - Новый акцентный цвет **Teal (#00d1b2)**.
  - Эффект **Glassmorphism** (матовое стекло) для сайдбаров и панелей.
  - Динамический **Tilt-эффект** (3D наклон) для карточек.
  - Кастомные плавные подсказки (Tooltips) для всех кнопок.
  - Эффект размытия (`blur`) основного фонда при открытии модальных окон.
- **Оптимизация верстки**:
  - Карточки переведены на строгий Flex-макет с фиксированным сайдбаром (60px).
  - Панель породы теперь отображается как абсолютный оверлей поверх фото (без прыжков верстки).
  - Полностью удалены все полосы прокрутки (`scrollbar`) во всем приложении.
  - Сетка галереи стала более компактной и адаптивной.

---
## [3.6.9] — 2026-05-15
### Добавлено
- **Smart URL Parsing (замена Gemini)**: Внедрена сверхбыстрая система распознавания породы на основе метаданных URL. Работает в 1000 раз быстрее ИИ, не требует VPN, ключей API и обладает 100% точностью.
- **Интеллектуальный словарь**: Локальная база данных из 100+ пород с автоматическим переводом на русский язык и коррекцией грамматических окончаний.
- **Manual Trigger**: Для экономии ресурсов и лучшего UX определение породы теперь запускается вручную по клику на кнопку «🤖 Определить породу».
- **Новый UI**: На карточках добавлена плашка с результатом распознавания в едином премиальном стиле.
- **Улучшенный просмотр**: В окне оценки фото теперь отображается полностью (`object-fit: contain`) с мягким размытием фона для создания глубины.
- **Премиум-редизайн**: Окно оценки переведено на современный сплит-макет (60/40), с полным удалением скроллбаров.

---

## [3.6.8] — 2026-05-15
### Добавлено
- **Глубокая тематизация**: Все жестко заданные цвета заменены на CSS-переменные. Интерфейс теперь полностью перестраивается при смене темы.
- **Цветовая гармония**: В светлой теме полностью удален черный цвет — он заменен на теплые темно-коричневые оттенки для соответствия эстетике проекта.

### Изменено
- **Модальные окна и уведомления**: Градиенты и фоны окон голосования, тостов и лайтбокса теперь динамически меняются вместе с темой.
- **Тени и границы**: Оптимизированы для лучшей читаемости в обоих режимах (коричневые тени для дня, черные/серые для ночи).

---

## [3.6.7] — 2026-05-15
### Добавлено
- **Темная тема**: Реализована полноценная поддержка темного режима.
- **Переключатель темы**: В правом верхнем углу добавлена кнопка (Солнце/Луна) для быстрой смены темы.
- **Память выбора**: Сайт запоминает выбранную тему и применяет её автоматически при следующем посещении.

### Изменено
- **Адаптивный дизайн**: Все элементы интерфейса (карточки, модальные окна, списки рейтинга) теперь динамически меняют цвета в зависимости от выбранной темы.

---

## [3.6.6] — 2026-05-15
### Добавлено
- **Направление сортировки**: Теперь можно выбирать порядок отображения в рейтинге (Высшие / Низшие). Это позволяет находить как самых «милых», так и самых «серьезных» пёсиков.
- **Общий балл**: Исправлен расчет и сохранение среднего арифметического по всем критериям (total score).

### Изменено
- **Дизайн фильтров**: Выпадающие списки в окне рейтинга получили новый премиальный дизайн с использованием шрифта 'Outfit' и мягких теней.
- **Стабильность**: Исправлена ошибка, из-за которой новые оценки могли не сразу отображаться в топе.

---

## [3.6.5] — 2026-05-15
### Глобальная ревизия и стабилизация
- **Объединение функций**: На карточки вернулись сердечки для «быстрой оценки» (лайков), при этом сохранена возможность детальной оценки по 5 критериям.
- **Новая структура Firebase**: Данные теперь организованы по полям `likes`, `ratings` (средние баллы) и `totalVotes`.
- **Двухколоночный рейтинг**: Окно рейтинга разделено на «По лайкам» и «По баллам» для удобства сравнения.
- **Визуальный отклик**: Кнопки меняют цвет (закрашиваются) после совершения действия, чтобы подтвердить принятие голоса.
- **Консольное логирование**: Добавлены уведомления «Данные успешно отправлены в Firebase» для упрощения отладки.
- **Оптимизация кода**: Проведена полная чистка от дубликатов, импорты сгруппированы в одном месте.

---

## [3.6.4] — 2026-05-15
### Добавлено
- **Многокритериальная оценка**: Система лайков заменена на полноценный вердикт по 5 параметрам: Кринжость, Размерность, Милость, Агрессивность и Красивность.
- **Интерактивные слайдеры**: Для оценки используется оверлей с плавными ползунками (1-10 баллов).
- **Средние баллы**: На карточках теперь отображаются средние значения по каждому критерию в реальном времени.
- **Расширенный рейтинг**: Добавлена сортировка топа по любому из 5 критериев или по общему баллу.
- **Защита от накрутки**: Один пользователь может вынести только один вердикт каждой собаке (состояние сохраняется в `voted_dogs`).

---

## [3.6.3] — 2026-05-15
### Добавлено
- **Система рейтингов**: Добавлена возможность просмотра лучших собак в специальном модальном окне.
- **Вкладки (Tabs)**: Переключение между «Общим топом» и личным списком «Мои лайки».
- **Сортировка**: Добавлен выбор порядка отображения (сначала популярные или наоборот).
- **Интерактив**: Возможность открыть полноразмерное фото прямо из списка рейтинга.

### Исправлено
- **Стили карточек**: Установлена фиксированная высота изображений (250px) и `object-fit: cover`. Теперь сетка идеально ровная.
- **Конфигурация**: Уточнены параметры Firebase (добавлен `measurementId`).

### Изменено
- **Отклик интерфейса**: Реализовано «оптимистичное обновление» лайков (мгновенная анимация).
- **Запуск**: Переход на локальный сервер для работы модулей.

---

## [3.6.2] — 2026-05-15
### Добавлено
- **Умные лайки**: Теперь лайк можно не только поставить, но и убрать (toggle).
- **Ограничение**: Один пользователь может поставить только один лайк на каждое фото.
- **Персонализация**: Состояние лайков сохраняется локально для каждого пользователя, при этом общее количество синхронизируется через Firebase.
- **Анимация**: Добавлен эффект пульсации (`heart-pop`) при нажатии на сердечко.

---

## [3.6.1] — 2026-05-15
### Добавлено
- **Firebase Firestore**: Полный переход на облачную базу данных для хранения лайков.
- **Синхронизация в реальном времени**: Использована функция `onSnapshot`. Лайки обновляются мгновенно у всех пользователей без перезагрузки страницы.
- **Безопасность**: Все ID изображений кодируются в `base64` для корректного хранения в Firestore.

### Изменено
- **Модульность**: Скрипт переведен на `type="module"`, импорты SDK выполняются через CDN.
- **Оптимизация**: Полностью удалена работа с `localStorage`.

---

## [3.6.0] — 2026-05-15
### Изменено
- **Тематический дизайн**: Сайт полностью стилизован под "собачью" тематику.
- **Цветовая палитра**: Использованы теплые коричневые и кремовые тона (Chocolate & Cream).
- **Анимации**: Добавлены "виляющие" лапки в заголовке.
- **Фон**: Добавлен стильный точечный паттерн, напоминающий текстуру.
- **Декор**: В заголовок и кнопку добавлены иконки лапок и косточек.

---

## [3.5.9] — 2026-05-15
### Добавлено
- **Лайтбокс**: Реализована возможность просмотра фотографий в полноэкранном режиме при клике на карточку.
- **Интерактив**: Добавлена анимация масштабирования (zoom) и размытие фона при открытии фото.
- **Удобство**: Закрыть фото можно нажатием на крестик, кликом по фону или клавишей `Esc`.

---

## [3.5.8] — 2026-05-15
### Изменено
- **Компактность**: Уменьшен общий размер сетки галереи и отступы между карточками для более компактного вида.
- **Сетка**: Сохранена структура из 3 колонок на компьютерах при уменьшенных размерах.

### Удалено
- Год (2024) из футера страницы для упрощения интерфейса.

---

## [3.5.7] — 2026-05-15
### Добавлено
- **Базовая структура**: Созданы файлы `index.html`, `style.css` и `script.js`.
- **Интеграция с API**: Подключен [Dog API](https://dog.ceo/api/breeds/image/random/9) для загрузки случайных фото.
- **Галерея**: Реализована адаптивная сетка (3 в ряд на ПК, 1 на мобильных).
- **Функционал**: Добавлена кнопка «Загрузить еще» для бесконечной подгрузки пёсиков без перезагрузки страницы.
- **Дизайн**: Современный стиль с тенями, скругленными углами и анимациями появления.
